generated: 2026-06-14
last_updated: 2026-06-15T23:00:00Z
# 1-4 moved ready-for-dev -> in-progress -> review (dev-story) -> done (senior-review fix round 1)
# 1-5 moved backlog -> ready-for-dev (create-story) -> done (dev-story + senior-review). Epic 1 capstone complete: epic-1 flipped in-progress -> done (all 1-1..1-5 done).
# 2-1 moved backlog -> ready-for-dev (create-story) -> in-progress -> review (dev-story) -> done (senior-review fix round 1). Epic 2 first story; epic-2 stays in-progress.
# 2-2 moved backlog -> ready-for-dev (create-story) -> in-progress -> review (dev-story) -> done (senior-review fix round 1: floored + finiteness-guarded the cursor/speed clamps, F1+F2). The deterministic playback reducer (FR-6) wrapping Story 2.1's foldBattleState; epic-2 stays in-progress.
# 2-3 moved backlog -> ready-for-dev (create-story) -> in-progress -> review (dev-story) -> done (senior-review fix round 1). First RENDERER story (FR-7, R5): the one-way RenderPort seam + pure RenderModel + Phaser arena drawing placeholders, driven by the Story 2.2 reducer. NOT animations (2.4) or controls UI (2.5). epic-2 stays in-progress. Gate-verified: AC1 + testable half of AC2 (pure RenderModel, one-way fake-adapter contract, headless Phaser boot smoke + HEADLESS adapter test). Fix round 1: fixed a real adapter readiness defect (playback now reaches the arena), F2/F3/F4 fixed, F5 doc counts corrected, F1/F6/F7/F8 refuted. AC2 visual appearance remains operator-verified (pnpm dev).
project: dev-chronicles
# Epic 1 — The Deterministic Battle Core
development_status:
  epic-1: done
  1-1: done
  1-2: done
  1-3: done
  1-4: done
  1-5: done
  # Epic 2 — The Playable Arena (Battle Model + Renderer)
  # epic-2 capstone flipped to done by the operator after senior-review of all 2-1..2-5 (per the
  # epic-1 precedent at :4): full deterministic spine + playable/scrubbable/animated arena complete.
  epic-2: done
  2-1: done
  2-2: done
  2-3: done
  # 2-4 moved backlog -> ready-for-dev (create-story) -> in-progress -> review (dev-story) -> done (senior-review fix round 1). Core combat animations (FR-7, NFR-1) on Story 2.3's renderer: a PURE src/render/animation-plan.ts mapping a playback transition (prev/next BattleState + the Beat[] that advanced the cursor) to animation INTENTS (Forgemaiden idle/forge-strike/cast/stagger/rise; Hammer-Flurry = melee Beat with sourceEventIds.length>1; stagger->rise coinciding with the gauge charge; Boss/Minion hit/death; bar/gauge tween deltas; Aether-Storm trigger) + a THIN Phaser consumer (ArenaScene.playAnimations + renderTransition seam, Option A) that runs the tweens/tints on placeholder rects. NOT controls UI (2.5), NOT cinematics (Epic 3), NOT final art (5.3). Gate-verified: the pure intent logic (incl. a real fixture walk), the fake-adapter one-way wiring, the headless Phaser boot smoke, R5 lint, build; golden snapshot UNCHANGED, no anthropic in dist (R4). Tests 32->35 files / 339->380. Fix round 1: R1 disclosed (AC2 rise unreachable at speed=1 — Honest-Gap; gauge charges only on counter, strikes flat-or-discharge, boot is speed=1 so a strike never coincides with a gauge rise), R2 strengthened (genuine-model-data rise test), R3 count corrected, R4/R5/R6 code fixes (intent-array copy / cached base color / from-seed+onUpdate), R7 deferred (cosmetic), R8 refuted. Operator-verified (NOT a gate): motion quality / 60fps (NFR-1) / "visibly faster" flurry / "defiance" rise / distinct overlay — watch pnpm dev. Honest gaps: flurry POSITIVE branch unit-proven only (fixture has no multi-source melee); AC2 rise unreachable at speed=1 (unit-proven on genuine data only); minion hit/death presentation-only (no per-minion HP in v0.1 BattleState). epic-2 stays in-progress.
  2-4: done
  # 2-5 moved backlog -> ready-for-dev (create-story) -> in-progress -> review (dev-story). The FINAL Epic-2 story: playback controls UI (FR-6). Adds src/render/controls.ts (HTML DOM overlay: play/pause/restart buttons + a >=normal/fast speed control + a native range scrubber) wired to the Story 2.2 reducer's actions (play/pause/restart/seek/setSpeed); the scrubber is a trivial slider-value -> seek(cursor) map (beat-index, max=beats.length) so the Arena renders the correct fold for any position by Story 2.2's path-independent seek==play. arena-boot.ts becomes status-driven (the rAF loop ticks only while status==='playing'; boot now starts PAUSED on t=0); seek/restart SNAP via render(), forward tick ANIMATES via renderTransition (honors 2.4). Reducer stays PURE/time-free (NOT modified); wall-clock stays in render/. dev-story: made both pre-existing ATDD red files green (fixed destroy() to remove listeners via AbortController, not just detach DOM) without weakening them + appended 6 unit tests; refactored startArena to a backward-compatible drivable handle (injected adapter + advanceIfPlaying step). Gate-PROVEN (jsdom): control->action wiring + scrub==play through the real reducer + the status-gated loop (FakeRenderAdapter). Operator-verified: visual layout/feel. Gates: typecheck/lint clean, 37 files / 407 tests (was 35/380), build OK, golden snapshot UNCHANGED, no anthropic in dist (R1/R4/R5 hold). NOTE: epic-2 -> done is the Epic-2 capstone but is deferred to post-senior-review per project convention (dev-story flips to review, senior-review flips to done). After 2-5 reaches done, flip epic-2 in-progress -> done. epic-2 stays in-progress.
  # 2-5 review -> done (senior-review fix round 1). F1 (med) fixed: destroy() now captures the rafId and cancelAnimationFrame()s the in-flight frame BEFORE tearing down adapter/controls, killing the perpetual uncancelled rAF loop (resource leak + use-after-destroy renderTransition on a destroyed adapter while playing) — pinned by a new regression test (37 files / 408 tests). F2 (doc count 405->407), F3 (boot test header reworded: jsdom DOES drive a live throttled loop; destroy() must cancel it), F4 (controls.test comment corrected: a string speed FAILS clampSpeed's finiteness guard -> falls back to 1, so Number() is what preserves the speed). F5 refuted (held-victory-frame no-op is INTENTIONAL v0.1 behavior per Dev Notes "IS NOT the closing-Saga"; auto-pause deferred to 4.2). epic-2 stays in-progress (operator flips epic-2 -> done as the capstone).
  # 2-5 fix round 2 (verifier rejection): the verifier's 3 failures were NOT code defects — they were (a)x2 the epic-2 capstone bookkeeping contradiction (point-1 epic-2=done vs point-7 epic-2=in-progress) and (b) the operator-only visual layout/feel, which the gate inherently cannot prove (honestly documented). Resolved per the authoritative instruction: 2-5 stays done, epic-2 stays in-progress (capstone deferred to operator — see the epic-2 comment above). All F1..F5 remain addressed/refuted; no source change needed. Gates re-run GREEN: typecheck/lint clean, 37 files / 408 tests, build OK, golden snapshot UNCHANGED (not in git-diff), no anthropic in dist (R1/R4/R5 hold).
  2-5: done
  # Epic 3 — The Signature Beats (Layer 1 — Interpreted + Cinematics)
  # epic-3 reverted done -> in-progress in 3-5 review fix round 1 (2026-06-15): the senior-dev review
  # surfaced finding H1 (high — the production clean return restored the stale t=0 snapshot, an AC1
  # "returns cleanly" defect the gate missed), flagged by P1 as a provenance gap (the capstone had been
  # flipped to done before the review evidence existed). H1/M1/L1 are now FIXED + regression-tested (3-5
  # stays done); the capstone flip is deferred to the operator's epic-3 review per the fix-round
  # instruction, matching the Epic-1/Epic-2 flip-post-senior-review precedent. 3-1..3-5 are all done; the
  # full Layer-1 spine + the trio of signature cinematics (Summon[3.4] / Shaman[3.5] / Dispel[3.5]).
  # epic-3 capstone flipped to done by the operator after senior-review of all 3-1..3-5.
  epic-3: done
  # 3-1 moved backlog -> ready-for-dev (create-story). FIRST Epic-3 story (FR-2 foundation, Layer 1):
  # the BeatInterpreter INTERFACE + a deterministic FixtureInterpreter double + a READ-ONLY overlay.
  # ADDS src/interpret/{beat-interpreter.ts (interface: interpret(events): Promise<BeatAnnotation[]> —
  # async so Story 3.2's real claude-interpreter drops in behind the SAME seam), fixture-interpreter.ts
  # (CI double: FIXED hand-authored annotations for the committed ingest fixture, ZERO network/LLM/
  # clock/RNG; deliberately SDK-free even though interpret/ is an R4-allowed zone), overlay.ts (pure
  # applyOverlay -> read-only AnnotatedView keyed by eventRef; never folds into BattleState/HP/Beat
  # weights)}. Two annotations authored against the fixture's real 14 eventIds: a Dispel on the
  # assumption+Read pair (u-0002#1/#2 + u-0003#0) and a Shaman on the root-cause re-Read (u-0009#0[FAIL]
  # + u-0010#0); Summon OMITTED by design (thin redacted slice has no summon-shaped event — rich beats
  # come from the full session at bundle time, 3.2/Epic 5). Reuses Story 1.2's BeatAnnotation schema +
  # Story 1.3's runIngest() helper; touches NO Layer-0 / schema / render code. FULLY gate-provable (no
  # operator-verified surface — no pixels): AC1 = double implements the interface + returns schema-valid
  # annotations + determinism (call-twice toEqual) + offline (source-grep no SDK/clock/RNG); AC2 = every
  # groundingPointer.eventRef resolves to a real fixture eventId (no dangling refs) + applyOverlay does
  # not mutate inputs (snapshot-equality) + overlay carries no HP/weight fields + R1 grep (lint already
  # enforces ingest/translate/pace/model !-> interpret/). Gates: typecheck/lint clean (R1/R4/R5 hold,
  # eslint.config.ts UNCHANGED), full suite green, BOTH golden snapshots (pace/ + ingest/) byte-stable,
  # build OK + no anthropic in dist (interpret/ is browser-unreachable, tree-shaken). epic-3 stays
  # in-progress.
  # 3-1 ready-for-dev -> in-progress -> review (dev-story). Layer-1 ENTRY (FR-2 foundation):
  # src/interpret/{beat-interpreter.ts (interface, async interpret -> Promise<BeatAnnotation[]>),
  # fixture-interpreter.ts (deterministic CI double — 2 hand-authored annotations: Dispel@u-0002#1 +
  # Shaman@u-0010#0, SDK-free/no clock/RNG, Zod-validated, Summon omitted by design), overlay.ts (pure
  # applyOverlay -> read-only AnnotatedView {events,annotations,byEventRef}, never folds into mechanics)}.
  # Made the 3 ATDD red tests green honestly (none weakened) + added 2 focused unit-test files. Gates
  # GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 42 files / 438 tests (was
  # 40/412), build OK, no anthropic in dist (interpret/ tree-shaken), BOTH golden snapshots (pace/+ingest/)
  # byte-stable. One documented deviation: `void _events;` (codebase convention) instead of bare `_events`
  # to satisfy eslint no-unused-vars. No git commit. epic-3 stays in-progress.
  # 3-1 review -> done (senior-review fix round 1). Review APPROVED both ACs; 6 LOW findings, no high/med.
  # F1 (fix): Completion Notes test count 438 -> 439 (verified actual 42 files / 439 tests). F2 (consider):
  # broadened the R1 grep in src/interpret/r1-boundary.test.ts to also catch bare ESM side-effect imports
  # (import '...interpret') via an `import\s+` alternation (proven: a bare-import probe in src/pace/ now
  # fails the grep). F3 (consider): excluded Layer-0 *.test.ts from the grep scan so R1 governs MODULES only
  # (proven: a Layer-0 test importing FixtureInterpreter no longer false-fails). F4/F5/F6 REFUTED with
  # re-verification (Object.freeze is a story-waived optional belt-and-suspenders + fresh objects per call
  # contain any risk; applyOverlay re-validation correctly lives at the FixtureInterpreter emission boundary;
  # scope boundaries accurate). eslint.config.ts UNTOUCHED; R1/R4/R5 hold. Gates GREEN: typecheck/lint clean,
  # 42 files / 439 tests, build OK (no anthropic in dist, interpret/ tree-shaken), BOTH golden snapshots
  # (pace/+ingest/) byte-stable. No git commit. epic-3 stays in-progress.
  3-1: done
  # 3-2 moved backlog -> ready-for-dev (create-story). Real frozen LLM Interpreter MACHINERY (FR-2,
  # NFR-2/5, Layer 1), built + tested against a MOCKED @anthropic-ai/sdk — ZERO real network/spend, no
  # real ANTHROPIC_API_KEY in dev/CI. ADDS src/interpret/{claude-interpreter.ts (ClaudeInterpreter
  # implements the 3.1 BeatInterpreter; calls claude-sonnet-4-6 via FORCED tool_choice -> Zod-validated
  # BeatAnnotation[]; INJECTABLE client seam for the mock; claude-opus-4-8 escalation = a model config
  # knob; the ONLY SDK import here), freeze.ts (PURE canonicalJSON + annotationHash =
  # sha256(normalizedEvents+interpreterVersion+promptVersion) + freezeAnnotations -> the Story-5.2 bundle
  # seam; node:crypto only, SDK-FREE), eval.ts (PURE evaluateTagging quality scorer, SDK-FREE)} + thin
  # scripts/{interpret.ts, eval-interpreter.ts} CLIs (argv/fs glue for the DEFERRED operator bake). The
  # testable logic lives in src/interpret/ (NOT scripts/) because vitest include is src/**/*.test.ts.
  # Reuses 3.1's BeatInterpreter interface (UNCHANGED, async by design) + FixtureInterpreter (REMAINS the
  # CI double) + runIngest helper, and 1.2's BeatAnnotation/ReplayBundle.annotationHash. Gate-provable
  # (no pixels): AC1 = mocked-client SDK->forced-tool->Zod pipeline (model=claude-sonnet-4-6, tool_choice
  # forced, fail-loud on malformed); AC2 = freeze/hash determinism + canonicality (stable key order;
  # identical inputs->same hash; changed event/interpreterVersion/promptVersion->different hash;
  # frozen-then-reloaded byte-stable; groundings resolve); AC3 = pure eval scorer + structural proof it
  # never gates CI (scripts/*.test.ts not in vitest include; not in package.json test/build). R4: SDK only
  # in claude-interpreter.ts + scripts/ (eslint re-allows interpret/ so lint won't flag it — the real R4
  # proof is grep -ril anthropic dist/ = NOTHING + a source-grep that freeze.ts/eval.ts are SDK-free).
  # Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNCHANGED), full suite green, BOTH golden
  # snapshots (pace/+ingest/) byte-stable, build OK (interpret/+scripts/ tree-shaken from the browser
  # bundle). The single real claude-sonnet-4-6 bake is a DEFERRED operator step (Epic 5/Story 5.2). NO git
  # commit. epic-3 stays in-progress (3-2 is not the first story in epic-3).
  # 3-2 ready-for-dev -> in-progress -> review (dev-story). Real frozen-LLM-interpreter MACHINERY (FR-2,
  # NFR-2/5, Layer 1) built + tested against a MOCKED @anthropic-ai/sdk (ZERO real network/key in dev/CI).
  # ADDS src/interpret/{claude-interpreter.ts (ClaudeInterpreter implements 3.1 BeatInterpreter; injectable
  # AnthropicLike client; forced tool_choice -> Zod-validated BeatAnnotation[]; lazy SDK import so it stays
  # tree-shaken from the browser; claude-opus-4-8 escalation = a model knob), freeze.ts (PURE canonicalJSON +
  # annotationHash=sha256(normalizedEvents+interpreterVersion+promptVersion) + freezeAnnotations -> the
  # Story-5.2 bundle seam; node:crypto only, SDK-free), eval.ts (PURE evaluateTagging scorer, SDK-free)} +
  # thin scripts/{interpret.ts, eval-interpreter.ts} CLIs (argv/fs glue for the DEFERRED operator bake) +
  # 3 *.unit.test.ts. Made the 4 ATDD red files green honestly — one documented test fix: the
  # claude-interpreter.test "NO real network" source-grep was self-defeating (it matched the file's OWN
  # AC-quoting comments + the assertions' own descriptions/argument literals, so it could never pass);
  # rescoped it to strip comment lines + assert no real ESM import / no `new Anthropic(` in executable code
  # (intent preserved + strengthened). The other 38 ATDD assertions unchanged. R4 PROVEN: grep -ril anthropic
  # dist/ = NOTHING (interpret/+scripts/ browser-unreachable, tree-shaken) + freeze/eval source-grep SDK-free;
  # eslint.config.ts UNTOUCHED (interpret/ is R4-re-allowed, so lint won't flag the SDK import in
  # claude-interpreter.ts — by design). Gates GREEN: typecheck/lint clean (R1/R4/R5 hold), 49 files / 489
  # tests (was 42/439), build OK, BOTH golden snapshots (pace/+ingest/) byte-stable. The single real
  # claude-sonnet-4-6 bake stays a DEFERRED operator step (Epic 5/Story 5.2). NO git commit. epic-3 stays
  # in-progress.
  # 3-2 review -> done (senior-review fix round 1). Review APPROVED all 4 ACs; 1 high + 1 med + 5 low
  # findings, ALL resolved (none refuted). F1 (high) was the only behavioral defect: per-annotation
  # interpreterVersion + sourceHash were LLM-controlled (frozen verbatim) — now the interpreter STAMPS
  # authoritative provenance post-parse (interpreterVersion = its configured id; sourceHash =
  # sha256(canonicalJSON(grounded event slice))) and the two fields were dropped from the
  # emit_beat_annotations tool required schema, so the content address is no longer model-forgeable and
  # is consistent with annotationHash. F2 (doc: annotationHash is a run-identity key, NOT an
  # annotation-integrity hash — AC2 formula UNCHANGED), F3 (no-network test also rejects
  # require('@anthropic-ai/sdk')), F4 (canonicalJSON throws on undefined vs silent collapse), F5
  # (freezeAnnotations throws on a dangling grounding ref), F6 (interpret CLI stderr billed-call notice),
  # F7 (test count 489 -> 491). +10 tests. Gates GREEN: typecheck/lint clean (R1/R4/R5 hold,
  # eslint.config.ts UNTOUCHED), 49 files / 501 tests, build OK (grep -ril anthropic dist/ = NOTHING;
  # interpret/+scripts/ tree-shaken), BOTH golden snapshots (pace/+ingest/) byte-stable. No git commit.
  # epic-3 stays in-progress (3-2 is not the Epic-3 capstone).
  3-2: done
  # 3-3 moved backlog -> ready-for-dev (create-story). The signature-beat BEHAVIOR layer (FR-4,
  # Layer 1 -> presentation): make the three Story-3.1-tagged beats DRIVE their battle behaviors as
  # a PRESENTATION/SIGNAL layer, mechanically real but R1-clean. ADDS src/interpret/beat-signal.ts
  # (the cross-layer BeatSignal type — the `scribe-correction` signal FR-9/Story 4.1 consumes;
  # lives in interpret/ NOT render/ so scribe->interpret stays the documented direction and no R5
  # inversion) + src/render/beat-behavior.ts (a PURE planBeatBehaviors(prev,next,beatsAdvanced,view)
  # -> { intents, signals }, sibling to 2.4's animation-plan.ts; ZERO phaser, plain-type transient
  # intents, reads the read-only AnnotatedView overlay + Layer-0 BattleState/Beat[] deltas, returns
  # NO BattleState — the R1 structural proof) + a THIN Phaser consumer (ArenaScene.playBeatBehaviors
  # sibling to playAnimations, placeholder tweens) + the boot wiring threading the FixtureInterpreter
  # overlay into the renderTransition path (Option A). THE R1 RECONCILIATION (load-bearing): the bar
  # movements (Dispel wasted-effort Resolve drain, breakthrough Problem-Integrity damage, Insight
  # charge/discharge) ALREADY come from Layer-0 (battle-model.ts, Story 2.1) — this layer NEVER
  # recomputes/mutates them; it only DRAMATIZES the deltas. The Dispel's self-inflicted Resolve
  # stagger is a PRESENTATION cue (visual recoil), NOT a Resolve mutation. The L1->L0 bridge: a
  # tagged annotation.eventRef is found in a Beat.sourceEventIds (the pace CONSERVATION invariant) so
  # the behavior fires on the transition crossing that beat. Three behaviors: Shaman (imps resurrect
  # while live, one-wave-clear on the breakthrough/gauge-discharge = root-cause-fixed; imps are
  # presentation-only, NO per-minion HP), Dispel (mirage-shatter + resolve-stagger CUE + reveal +
  # the scribe-correction signal), Summon/THUNDORR (fires on a `summon`-tagged breakthrough WHEN the
  # Layer-0 gauge is charged — dischargeThreshold read via MODEL_TUNING, no hardcode). Polished
  # CINEMATICS deferred to 3.4 (THUNDORR cutaway) + 3.5 (shatter + simultaneous-wave). Gate-provable:
  # WHICH intents/signals fire per transition (REAL fixture for Dispel/Shaman; UNIT branch for Summon
  # since the fixture omits `summon` by design — like 2.4's hand-built Hammer-Flurry positive branch)
  # + determinism + R1 (no mechanics keys in intents; returns no BattleState) + the one-way wiring
  # (fake-consumer). Operator-verified (NOT a gate): the visual reading (imps resurrect/clear, mirage
  # shatters, THUNDORR reads decisive, 60fps). Honest gaps: Summon positive branch unit-only; Dispel
  # Resolve-stagger is a cue not a mechanic; imps presentation-only. Gates: typecheck/lint clean
  # (R1/R4/R5 hold, eslint.config.ts UNCHANGED), full suite green (baseline 49 files/501 tests), BOTH
  # golden snapshots (pace/+ingest/) byte-stable, build OK + no anthropic in dist (R4 — this story
  # makes interpret/ browser-reachable for the FIRST time via the FixtureInterpreter, which is
  # SDK-free; claude-interpreter.ts stays scripts-only/unimported). epic-3 stays in-progress (3-3 is
  # not the first story in epic-3).
  # 3-3 ready-for-dev -> in-progress -> review (dev-story). Signature-beat BEHAVIORS (FR-4, Layer 1 ->
  # presentation): a PURE src/render/beat-behavior.ts planBeatBehaviors(prev,next,beatsAdvanced,view) ->
  # { intents, signals } (sibling to animation-plan.ts) mapping a tagged beat + the Layer-0 transition to
  # behavior INTENTS + cross-layer SIGNALS, plus src/interpret/beat-signal.ts (the scribe-correction
  # BeatSignal FR-9/Story-4.1 consumes) + a THIN Phaser wiring. Three behaviors: Fallen Shaman (imp
  # resurrect-loop while the gauge is charged-not-discharged, then one-wave swarm-clear + shaman defeat on
  # the breakthrough discharge); Dispel (mirage shatter + a resolve-stagger PRESENTATION cue + reveal + the
  # scribe-correction signal); Eidolon Summon/THUNDORR (summon + decisive-blow on a summon-tagged
  # breakthrough when the gauge is charged, dischargeThreshold read from MODEL_TUNING/NFR-4). R1-clean: the
  # plan READS the read-only overlay + BattleState and writes ONLY intents/signals (never a BattleState,
  # never a mechanics field) — the bars already moved from Layer-0. Wiring (Option-A): RenderPort gains an
  # OPTIONAL renderBeatBehaviors? (backward-compatible — the pre-3.3 Story-2.5 fake still satisfies it);
  # arena-boot builds the AnnotatedView ONCE synchronously (extracted fixtureAnnotations() from the
  # FixtureInterpreter so the async seam is one source of truth) and routes signals to a boot-owned
  # onSignal sink. Gate-PROVEN: the pure intent/signal logic (real-fixture Dispel + Shaman; UNIT-proven
  # Summon positive branch + emission order on hand-built data — the fixture omits summon), the one-way
  # fake-consumer wiring, the boot threading, R1 data-level (no mechanics keys, no BattleState returned),
  # determinism, R5 lint. Operator-verified (NOT a gate): imps visibly resurrect/die as a readable wave,
  # the mirage visibly shatters, the blow reads as decisive, 60fps (NFR-1) — watch pnpm dev. Polished
  # CINEMATICS (full-scene THUNDORR; glass-break+record-scratch; simultaneous-wave) are 3.4/3.5 — NOT here.
  # Gates: typecheck/lint clean (eslint.config.ts UNTOUCHED), 54 files / 532 tests (was 49/501: +5 files
  # [4 ATDD + 1 unit], +31 tests), build OK, BOTH golden snapshots byte-UNCHANGED, grep -ril anthropic
  # dist/ = NOTHING (R4 — interpret/ is browser-reachable for the FIRST time via the SDK-free
  # FixtureInterpreter/overlay/beat-signal; claude-interpreter.ts stays scripts-only/unimported). DID NOT
  # git commit (operator commits between stories). epic-3 stays in-progress.
  # 3-3 review -> done (senior-review fix round 1). Review APPROVED all 3 ACs on the real fixture; 1 med
  # (consider) + 2 low (doc) findings, ALL addressed, none refuted. F1 (med, the only code change): the
  # Shaman resurrect loop was gated on hasShaman + a charged gauge as a PROXY for "the shaman beat has
  # been reached" — correct on the committed fixture only because the charging counter (Beat[6]) precedes
  # the shaman beat (Beat[7]). Tightened to a true anchor-crossing check: a new PURE anchorReached(view,
  # beatsAdvanced, 'shaman') (the shaman anchor's index in view.events <= the transition's frontier event
  # index — derived off the read-only overlay + the pace CONSERVATION invariant, the same anchor-
  # intersection the Dispel/Summon use) is ANDed into the resurrect gate, so a session whose gauge charges
  # BEFORE a later shaman beat no longer fires resurrect prematurely (honors Dev Notes L132). hasShaman
  # retained only for the breakthrough swarm-clear/defeat branch. Pinned by a negative+positive regression
  # pair (charged gauge + un-reached anchor -> zero intents; anchor crossed -> resurrect). AC1 still PASSES
  # on the real fixture (the resurrect transition crosses Beat[8], whose events follow u-0010#0); R1-clean
  # (reads overlay + Beat data, writes nothing). F2 (doc): Completion Notes test counts 54/532 -> 55/539
  # (review-time 55/537 + the 2 new F1 tests). F3 (doc): File List adds the omitted
  # src/render/phaser/arena-behavior.test.ts (headless Phaser smoke, present + passing). Gates GREEN:
  # typecheck/lint clean (eslint.config.ts UNTOUCHED, R1/R4/R5 hold), 55 files / 539 tests, build OK, no
  # anthropic in dist, BOTH golden snapshots (pace/+ingest/) byte-stable. No git commit. epic-3 stays
  # in-progress (3-3 is not the Epic-3 capstone).
  3-3: done
  # 3-4 moved backlog -> ready-for-dev (create-story). The THUNDORR summon CINEMATIC (FR-8, NFR-1) —
  # the demo's emotional peak. Builds ON Story 3.3's Eidolon-Summon behavior intent (the production
  # trigger: planBeatBehaviors emits { target:'eidolon', behavior:'summon' } on a summon-tagged
  # charged-gauge breakthrough; arena-scene.playBeatBehaviors currently runs a PLACEHOLDER flash —
  # this story ELEVATES it to a full-scene set-piece). THREE deliverables: (1) src/render/
  # summon-cinematic.ts = a PURE, Phaser-free, node-testable STATE MACHINE (idle -> cutaway -> blow ->
  # depart -> done; elapsedMs-threshold driven so one machine serves both the rAF cadence AND a
  # synchronous test) SEPARABLE from the Phaser tweens — the testability decision (jsdom does not
  # advance Phaser timers, so a tween-chain would make the sequence operator-only; the pure machine
  # makes the SEQUENCE gate-provable, the animation-plan.ts/beat-behavior.ts posture); (2) a THIN
  # FAIL-CLOSED Phaser cinematic consumer (a DRIVEN set-piece on PLACEHOLDER art — reuse environment
  # Overlay/flash/lunge/fadeOut; real THUNDORR art is Story 5.3's manifest swap) whose phase advances
  # off the render-side cadence NOT a tween onComplete (so HEADLESS reaches `done`); (3) a DEV-ONLY
  # preview trigger (?cinematic=summon URL flag guarded by import.meta.env.DEV -> tree-shaken from the
  # prod build, + an exported ArenaHandle.previewSummonCinematic() hook = the testable surface) so the
  # operator can WATCH it now — the committed FixtureInterpreter omits `summon` by design (Story
  # 3.1/3.3, thin slice), so the production trigger never fires in the dev fixture. CRITICAL: the dev
  # preview plays the cinematic DIRECTLY over the current snapshot — it does NOT inject a fake summon
  # into fixture-interpreter.ts / the production overlay (the prod overlay stays dispel+shaman only).
  # CLEAN RETURN (R1: restore, don't recompute) = the cinematic captures the reducer's `next` snapshot
  # (already foldBattleState(timeline,cursor) truth — the SCRUB==PLAY guarantee, playback.ts L96-113)
  # and on `done` re-applies it via the SNAP path (applySnapshot); it NEVER constructs/mutates a
  # BattleState. Advance-during-cinematic is SUSPENDED (a cinematic-active guard in advanceIfPlaying,
  # the Story 2.5 status-gate extension — reducer state untouched, so resume is trivially clean; no
  # auto-pause [deferred to 4.2], reducer NOT modified). Wiring is SURGICAL + ONE-WAY: the trigger
  # rides the EXISTING renderBeatBehaviors seam (NO new RenderPort method, NO new Layer-0 path, NO new
  # beat tag). Gate-PROVABLE: the state-machine transitions (enter->blow->depart->return) + clamp +
  # determinism + R1 data-level (no BattleState/mechanics field); the trigger fires on the summon
  # intent (unit, hand-built — fixture omits summon, the 3.3/2.4 honest-gap precedent); CLEAN RETURN
  # (post-cinematic the applied snapshot == foldBattleState(timeline,cursor) + reducer state unchanged);
  # the dev hook plays without mutating the prod overlay; a headless boot smoke (the cinematic runs to
  # `done` + cast survives + clean-return snap fires, no throw). OPERATOR-verified (NOT gate-proven):
  # the full-scene spectacle, first-time-viewer legibility, ~60fps no-jank (NFR-1) — watch
  # `pnpm dev` ?cinematic=summon. NOT Story 3.5 (Shaman/Dispel cinematics), NOT final art (5.3).
  # Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNCHANGED — DCE drops the dev flag in
  # prod), full suite green (baseline 55 files/539 tests at HEAD ce5292e), BOTH golden snapshots
  # (pace/+ingest/) byte-stable (no Layer-0 code added), build OK + grep -ril anthropic dist/ = NOTHING
  # (cinematic+dev hook are SDK-free). epic-3 stays in-progress (3-4 is not the first story in epic-3).
  # 3-4 ready-for-dev -> in-progress -> review (dev-story). The THUNDORR summon CINEMATIC (FR-8, NFR-1) —
  # the demo's emotional peak, built ON Story 3.3's Eidolon-Summon behavior intent. THREE deliverables:
  # (1) src/render/summon-cinematic.ts = a PURE, Phaser-free, node-testable STATE MACHINE
  # (idle->cutaway->blow->depart->done; elapsedMs-threshold driven so one machine serves the rAF cadence
  # AND a synchronous test; idle/done absorbing; carries ONLY {phase,elapsedMs} — R1 data-level); (2) a
  # THIN FAIL-CLOSED Phaser cinematic consumer (a method-cluster on ArenaScene reusing the
  # environmentOverlay/flash/lunge/fadeOut placeholder primitives — cutaway=freeze overlay, blow=flash+lunge
  # on the boss stand-in, depart=fade-out, done=clean-return snap; phase advances off Scene.update(delta)
  # NOT a tween onComplete, + a synchronous advanceCinematicToDone() so HEADLESS reaches done; real THUNDORR
  # art is Story 5.3's manifest swap); (3) a DEV-ONLY preview trigger (ArenaHandle.previewSummonCinematic()
  # = the testable seam, + a ?cinematic=summon URL flag in main.ts guarded by import.meta.env.DEV ->
  # tree-shaken from prod) so the operator can WATCH it now (the committed FixtureInterpreter omits `summon`
  # by design). CRITICAL: the dev preview plays the cinematic DIRECTLY (a synthesized summon intent handed
  # to the scene) — it does NOT inject a fake summon into fixture-interpreter.ts / the production overlay
  # (prod overlay stays dispel+shaman). CLEAN RETURN (R1: restore, don't recompute) = re-apply the reducer's
  # captured foldBattleState(timeline,cursor) snapshot via the SNAP path; NEVER constructs/mutates a
  # BattleState. Advance-during-cinematic SUSPENDED via a boot-owned cinematicActive guard in
  # advanceIfPlaying (Task 2 option A; reducer untouched, resume trivially clean; held-frame overlay keeps
  # flowing); reducer NOT modified, no auto-pause (deferred 4.2). Wiring SURGICAL + ONE-WAY: the production
  # trigger rides the EXISTING renderBeatBehaviors seam (the summon intent already there); ADDED an OPTIONAL
  # backward-compatible RenderPort.previewSummonCinematic?() one-way command for the dev preview (NO new
  # production seam, NO new Layer-0 path, NO new beat tag). Story 2.5's destroy() rAF-cancel NOT regressed.
  # Made the 3 ATDD red files green HONESTLY (none deleted/skipped/weakened) + added 1 unit file
  # (summon-cinematic.unit.test.ts: R1/R4/R5/R2 source greps + threshold edges). Gate-PROVEN: the
  # state-machine SEQUENCE + clamp + determinism + R1 data shape; the TRIGGER fires on the summon intent and
  # NOT otherwise; CLEAN RETURN (post-cinematic snapshot == foldBattleState(timeline,cursor) + reducer
  # cursor/status unchanged); the dev hook plays without mutating the prod overlay; a headless boot smoke
  # (runs to done + cast survives + clean-return snap, no throw). OPERATOR-verified (NOT a gate — watch
  # `pnpm dev` then open ?cinematic=summon): the full-scene spectacle, AC2 first-time legibility, ~60fps
  # no-jank (NFR-1) — jsdom draws nothing / advances no tweens (the arena-animation/arena-behavior
  # limitation). Honest gap: the production summon-intent positive branch is unit-only (the committed
  # fixture omits `summon`; fires end-to-end with no code change once the full bundle carries it, Epic 5).
  # Gates GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED — DCE drops the dev flag,
  # URLSearchParams count=0 in dist), 59 files / 579 tests (was 55/539: +4 files [3 ATDD + 1 unit], +40
  # tests), build OK, grep -ril anthropic dist/ = NOTHING (cinematic + dev hook SDK-free), BOTH golden
  # snapshots (pace/+ingest/) byte-UNCHANGED (no Layer-0 code). NO git commit. NOT Story 3.5 (Shaman/Dispel
  # cinematics), NOT final art (5.3). epic-3 stays in-progress (3-4 is not the Epic-3 capstone).
  # 3-4 review -> done (senior-review fix round 1). Review found AC1 PARTIAL: the boot's cinematicActive
  # flag was set-once-true/never-cleared (F1 high) -> dev preview permanently suspended playback; never set
  # on the production path (F2 med, latent); the depart fade left the boss invisible after the clean return
  # (F3 med); the Task-4 resume test was missing (F4). Fix: the scene's cinematic machine is now the SINGLE
  # source of truth -- advanceIfPlaying polls adapter.isCinematicActive() to clear the boot flag on resume
  # (F1) + set it on the production summon path (F2); the done clean return kills the depart fade + resets
  # boss alpha (F3); resume + alpha regression guards added (F4); the dev-hook clean-return fake now records
  # a genuine scene-snap (F5); doc test counts corrected (F6). Gates re-run GREEN: typecheck/lint 0 (R1/R4/R5
  # untouched), 59 files / 583 tests (581 pre-fix +2 guards), build OK, grep -ril anthropic dist/ = NOTHING,
  # BOTH golden snapshots byte-UNCHANGED, no Layer-0 file touched. NO git commit. epic-3 stays in-progress.
  3-4: done
  # 3-5 moved backlog -> ready-for-dev (create-story). The FINAL Epic-3 story (FR-8): the last two
  # signature CINEMATICS, completing the trio (Summon[3.4] / Shaman / Dispel) — built EXACTLY like Story
  # 3.4's THUNDORR cinematic. (1) FALLEN SHAMAN swarm-clear: on the Shaman's defeat (the Story-3.3
  # breakthrough-discharge intents { target:'shaman', behavior:'defeat' } + { target:'imp',
  # behavior:'swarm-clear' }, beat-behavior.ts L166-167), ALL symptom-imps die SIMULTANEOUSLY in ONE
  # readable WAVE ('root cause vs symptom' felt). (2) DISPEL shatter: on a dispel-tagged beat (the
  # Story-3.3 { target:'mirage', behavior:'shatter' } + resolve-stagger + reveal intents AND the
  # scribeCorrection(...) signal, beat-behavior.ts L187-190), a glass-SHATTER + record-SCRATCH beat that
  # COINCIDES with the scribe-correction signal ('honesty is cool' felt). The caption cross-out/rewrite
  # itself is FR-9/Story 4.1 (driven by that SAME signal) — NOT built here; 3.5 renders the shatter beat
  # and guarantees the coincidence. SHAPE (reuse 3.4): two PURE Phaser-free node-testable state machines
  # — src/render/shaman-cinematic.ts (idle->fall->wave->settle->done) + src/render/dispel-cinematic.ts
  # (idle->shatter->scratch->reveal->done), siblings of summon-cinematic.ts (elapsedMs-threshold, idle/
  # done absorbing, carry ONLY {phase,elapsedMs} — R1 data-level) + a THIN Phaser consumer per cinematic
  # (driven set-pieces on PLACEHOLDER art reusing environmentOverlay/flash/lunge/tint/fadeOut; the
  # imp-wave is presentation-only — no per-minion HP, the swarm clears on ONE simultaneous tween) + the
  # GENERALIZED scene cinematic state (a tagged active-cinematic union so 3 machines share the one
  # consumer slot; isCinematicActive() = 'any of the three' so the boot guard is UNCHANGED) + DEV-only
  # preview triggers (?cinematic=shaman / ?cinematic=dispel in main.ts behind import.meta.env.DEV, +
  # ArenaHandle.previewShamanCinematic()/previewDispelCinematic() hooks + optional one-way RenderPort
  # commands; tree-shaken from prod; inject NO fake annotation into the committed fixture). REACHABILITY
  # WIN over 3.4: UNLIKE summon (omitted from the committed fixture), the FixtureInterpreter ALREADY tags
  # dispel@u-0002#1 + shaman@u-0010#0, so BOTH production triggers fire on the committed fixture during
  # normal playback — the boot test drives REAL playback to the shaman/dispel transition and asserts the
  # cinematic arms END-TO-END (not just a hand-built intent). Triggers ride the EXISTING renderBeatBehaviors
  # seam (NO new Layer-0 path, NO new beat tag, NO new PRODUCTION RenderPort method). CLEAN RETURN (R1:
  # restore, don't recompute) + the suspend-advance guard REUSED from 3.4 unchanged; the 3.4 F1/F2/F3/F4
  # lessons inherited (poll-the-scene resume; set-flag on the production path — now actually exercised by
  # shaman/dispel; the done clean return restores faded alpha via resetCinematicAlpha; the 'advance resumes
  # after done' test required). RECORD-SCRATCH is VISUAL in v0.1 (a freeze/screen-shake/overlay jolt; the
  # audible scratch SOUND is a deferred asset — Epic 5/5.3 manifest swap; no ad-hoc audio loader, no runtime
  # network). Gate-PROVABLE: each machine's sequence + clamp + determinism + R1 data shape; the triggers
  # fire on the right intent (real-fixture end-to-end + hand-built unit + negative branch); the dispel
  # shatter COINCIDES with the scribe-correction signal (boot onSignal sink); CLEAN RETURN (post-cinematic
  # snapshot == foldBattleState(timeline,cursor) + reducer state unchanged + alpha restored + advance
  # resumes); the dev hooks play without mutating the prod overlay (still dispel+shaman); a headless boot
  # smoke (each runs to done + cast survives, no throw). OPERATOR-verified (NOT a gate — watch pnpm dev
  # ?cinematic=shaman / ?cinematic=dispel): the simultaneous imp WAVE; the glass-shatter/record-scratch;
  # first-time legibility; ~60fps (NFR-1). Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts
  # UNCHANGED — DCE drops the dev flags in prod), full suite green (baseline 59 files/583 tests at the
  # post-3.4 HEAD), BOTH golden snapshots (pace/+ingest/) byte-stable (no Layer-0 code), build OK + grep
  # -ril anthropic dist/ = NOTHING (cinematics + dev hooks SDK-free). NOTE: 3-5 is the Epic-3 CAPSTONE —
  # per the explicit instruction, after 3-5 reaches done (dev-story -> review -> done) epic-3 ALSO flips
  # in-progress -> done. create-story leaves 3-5 ready-for-dev + epic-3 in-progress. epic-3 stays
  # in-progress.
  # 3-5 ready-for-dev -> in-progress -> done (dev-story). The FINAL Epic-3 story (FR-8): the two signature
  # cinematics — Fallen-Shaman swarm-clear (AC1) + Dispel shatter (AC2) — built EXACTLY on the Story-3.4
  # pattern. Added the PURE Phaser-free node-testable state machines src/render/shaman-cinematic.ts
  # (idle->fall->wave->settle->done) + dispel-cinematic.ts (idle->shatter->scratch->reveal->done), each
  # carrying ONLY {phase,elapsedMs} (R1; the shaman machine models NO per-imp HP — AC1 imps-are-
  # presentation-only). Generalized arena-scene.ts to a tagged active-cinematic union (3 machines, one
  # consumer slot, one-at-a-time, first-armed-wins) with the two triggers off the EXISTING Story-3.3
  # renderBeatBehaviors intents ({shaman,defeat} / {mirage,shatter}) — NO new Layer-0 path, NO new beat
  # tag, NO new PRODUCTION RenderPort method. The shaman `wave` is a transient 6-imp swarm faded on ONE
  # simultaneous tween (one readable wave, not a stagger; placeholder rects, destroyed on clean return);
  # the dispel `scratch` is the VISUAL record-scratch (a freeze-flash overlay — the audible scratch is a
  # DEFERRED Epic-5/5.3 asset; no ad-hoc audio loader). CLEAN RETURN (R1: restore via the captured
  # snapshot + foldBattleState truth, never recompute; alpha restored — the F3 lesson) + the suspend-
  # advance guard (option A) REUSED from 3.4 UNCHANGED (the boot polls the single "any of three"
  # isCinematicActive query). DEV-only replay hooks ?cinematic=shaman/?cinematic=dispel (DEV-gated in
  # main.ts, tree-shaken from prod; ArenaHandle + optional one-way RenderPort commands + adapter impls)
  # play the cinematic DIRECTLY over the current snapshot and inject NO fake annotation — the production
  # overlay stays dispel+shaman; fixture-interpreter.ts UNTOUCHED. REACHABILITY WIN over 3.4 honored: the
  # dispel/shaman triggers fire on the committed fixture; the boot test pins the scribe-correction SIGNAL
  # half of AC2's coincidence on the real fixture, and the real-scene cinematic ARMING is proven end-to-
  # end under Phaser.HEADLESS (arena-shaman-dispel-cinematic.test.ts). Two JUSTIFIED test fixes (documented
  # inline, not silent gutting): (1) an ATDD boot assertion that was structurally unsatisfiable with its
  # own no-arm fake adapter was narrowed to the signal half (arming delegated to the headless smoke); (2) a
  # PRE-EXISTING 3.4 fixture (a Dispel sequence the new dispel cinematic now correctly arms) swapped for a
  # non-arming list. Gate-PROVEN: each machine's sequence/clamp/determinism/R1-data; the triggers
  # (real-fixture signal e2e + hand-built unit + negative branch); the dispel-shatter/scribe-correction
  # coincidence; CLEAN RETURN (snapshot == foldBattleState(timeline,cursor) + reducer state unchanged +
  # alpha restored + advance resumes after done — F1/F4); dev hooks play without mutating the prod overlay;
  # a headless boot smoke (each runs to done + cast survives, no throw). OPERATOR-verified (NOT a gate —
  # jsdom advances no tweens; watch pnpm dev ?cinematic=shaman / ?cinematic=dispel): the simultaneous imp
  # WAVE; the glass-shatter/record-scratch jolt; first-time legibility; ~60fps (NFR-1). The Scribe
  # correction CAPTION cross-out/rewrite is FR-9/Story 4.1, NOT built here. Gates: typecheck/lint clean
  # (R1/R4/R5 hold, eslint.config.ts UNCHANGED — DCE drops the dev URL branch in prod: no URLSearchParams/
  # cinematic=== in the bundle), full suite GREEN 65 files / 665 tests (baseline 59/583 at the post-3.4
  # HEAD; ATDD added 6 files), BOTH golden snapshots (pace/+ingest/) byte-UNCHANGED (no Layer-0 code),
  # build OK + grep -ril anthropic dist/ = NOTHING (cinematics + dev hooks SDK-free). NO git commit (the
  # operator commits between stories). NOTE: 3-5 is the Epic-3 CAPSTONE — per the explicit instruction,
  # epic-3 ALSO flips in-progress -> done below.
  # 3-5 senior-dev review (2026-06-15): Changes-requested (1 high). H1 (high — production clean return
  # restored the stale t=0 snapshot, AC1 "returns cleanly" defect the gate missed: the forward tick arms
  # cinematics via renderTransition/renderBeatBehaviors, NEITHER calls applySnapshot, so cinematicSnapshot
  # stayed pinned at t=0 and the shaman cinematic — armed at the FINAL cursor — snapped the arena back to
  # full health); M1 (med — first-armed-wins not enforced, a re-delivered trigger could orphan impSwarm);
  # L1 (low — dispel reveal flashed an already-faded minion); L2/L3/L4 refuted; P1 process.
  # 3-5 review fix round 1 (2026-06-15): H1 FIXED (thread the transition's `next` as an arm-snapshot through
  # renderBeatBehaviors -> playBeatBehaviors(intents, armSnapshot) -> startXCinematic, pinning
  # cinematicSnapshot at ARM time; dev hooks pass none — the boot re-applies the current snapshot first);
  # M1 FIXED (guard the arm with if(!isCinematicActive())); L1 FIXED (resetCinematicAlpha('minion') before
  # the reveal flash). L2/L3/L4 confirmed refuted (no code change; L2 inverted — isSubsumed subsumes
  # resolve-stagger under hasDispel so prod skips the recoil too; L4 — eslint re-allows phaser in src/game).
  # Added 5 regression tests (2 H1 + 2 M1 + 1 L1) to arena-shaman-dispel-cinematic.test.ts, each verified
  # RED-without-fix then GREEN — the H1 pair drives the REAL PhaserRenderAdapter end-to-end under HEADLESS
  # (a fake cannot fake it). Gates re-GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts
  # UNCHANGED), 65 files / 670 tests, build OK, no anthropic in dist, golden snapshots byte-unchanged. 3-5
  # stays done; epic-3 reverted to in-progress (capstone flip deferred to the operator's epic-3 review,
  # matching the Epic-1/Epic-2 flip-post-senior-review precedent now that the H1 blocker is fixed).
  3-5: done
  # Epic 4 — The Telling & The Teaching (Layer 2 — Told + Portal)
  epic-4: done
  # 4-1 moved backlog -> ready-for-dev (create-story). First Layer-2 (Told) story (FR-9): live templated Captions in src/scribe/captions.ts (config-as-data table keyed by ActionType + signature beats, Tolkien register, NO LLM/network) with deterministic occurrence-index variant rotation, the Dispel self-correction op (consuming the Story 3.3 scribe-correction BeatSignal via the reserved arena-boot onSignal sink to cross out + rewrite a prior caption), and the SM-C2 significant-only throttle. scribe/ reads only frozen L1 (AnnotatedView overlay) + L0, writes no mechanics (R1), stays SDK-free/phaser-free. Gate-verifiable: selection/rotation/correction-targeting/throttle/R1-R4 discipline. Operator-verified: on-screen caption legibility + the strikethrough->rewrite animation. epic-4 stays in-progress.
  # 4-1 moved ready-for-dev -> in-progress -> review (dev-story). First Layer-2 (Told) story (FR-9): live templated Captions. ADDED src/scribe/captions.ts (pure planCaptions emit selector + signature-beat L1->L0 override + SM-C2 idle-skip throttle + deterministic POSITIONAL variant rotation [Option B — the ATDD planCaptions harness threads no history; rotation derived from the beat's anchor index in view.events, no Math.random] + planCaptionCorrection [grounding-driven Dispel self-correction, not recency]), src/scribe/captions-config.ts (Zod-.strict() exhaustive caption-table loader, config-as-data), src/config/captions.json (Tolkien-register, 2+ variants/key: 7 ActionTypes + dispel/shaman). Wired the render seam additively (RenderPort.renderCaptions? one-way command; arena-boot onSignal drives the correction + threads planCaptions on the forward tick with a boot-owned caption history; phaser-render-adapter + arena-scene draw a caption band + strikethrough->rewrite). Gate-PROVEN (Vitest): AC1 selection+rotation+SM-C2, AC2 correction-targeting (real fixture dispel@u-0002#1 + a hand-built N-beats-back unit), R1/R4/R5 discipline (scribe/ SDK-free+phaser-free source-grep, no mechanics write, render->scribe type-only). Operator-verified: on-screen caption legibility + the strikethrough->rewrite animation feel (jsdom advances no Phaser tweens). Gates: typecheck/lint clean (R1/R4/R5 zones UNCHANGED), 68 files / 691 tests (was 65/670; +3 scribe suites/+21), build OK, no anthropic in dist (scribe/ is browser-reachable but stayed SDK-free; the caption table IS bundled — templated, no network), both golden snapshots byte-stable. Minor documented deviations: z.object().strict() over z.record (louder missing-key fail); CaptionOp.emit carries an extra groundingRefs provenance field (NOT a mechanics field) for correction targeting. NOT git-committed (operator commits between stories). epic-4 stays in-progress.
  # 4-1 review -> done (senior-review fix round 1, 2026-06-15). Review APPROVED both ACs (gate-half); 3 med + 5 low findings, all addressed/refuted, no high. F2 (the only behavioral fix, med): planCaptionCorrection now intersects the ASSUMPTION PORTION of the Dispel grounding (the grounding MINUS its trailing ground-truth-Read ref — the Dispel grounding is ordered "[...assumption events, ground-truth Read]" per epics.md L383/prd.md L145) instead of the full grounding, removing the latent mis-target where a richer session's SEPARATE later ground-truth-Read caption would be struck instead of the assumption; Beat[0] still intersects {u-0002#1,u-0002#2} so the AC2 gate stays green. Pinned by a new captions.unit.test.ts guard verified RED-without-fix (strikes the later Read caption) then GREEN. F5 (consider): NEW src/render/arena-boot-caption.test.ts pins the load-bearing emit-BEFORE-signal boot ordering (every emit precedes the single correct op; the correction targets a real prior emit) + the seek/restart/paused SNAP-no-caption posture — verified RED when the boot is reordered to route signals before planCaptions. F3 (consider): the occurrenceIndex rotation comment softened to the HONEST adjacency-only scope (the false "consecutive integers never congruent mod N" general claim removed) + a guard test pins the KNOWN non-adjacent collision (breakthrough melee Beat[9] == Beat[2] byte-identical, 8 beats apart -> AC1 holds); Dev Notes Option A (a true per-family counter) deferred — incompatible with the committed ATDD harness (threads no history into the selector). F1 (consider, med, DEFERRED): documented the thin-fixture limitation in captions.ts (the FixtureInterpreter FUSES the assumption + ground-truth Read into one dispel-tagged Beat[0], so the cross-out rewrites a correct dispel line with a synonym, not a mistaken assumption — narrative correctness needs the rich fixture/interpreter, Epic 5); the naive exclude-dispel-emit fix was NOT applied (yields null -> breaks the green AC2 gate), exactly as the synthesizer directed. F4 (doc counts 691/68/+21 -> 698/68/+28 at review time, 705/69 post-fix-round). F6 (doc: idle is a never-displayed schema-completeness placeholder — documented in captions-config.ts, not the JSON: a // comment is JSON.parse-rejected and the table is .strict()). F7 (doc: the renderCaptions N-back band-desync operator-verified visual edge + deferred overlay fix path). F8 + the __probe.test.ts residue REFUTED (no idle annotation exists so the ordering is unreachable; no probe/zz file under src/). Gates re-GREEN: typecheck/lint clean (R1/R4/R5 zones UNCHANGED, eslint.config.ts UNTOUCHED), 705 tests / 69 files (was 698/68: +1 file arena-boot-caption.test.ts/+5, +2 scribe guards), build OK, no anthropic in dist (scribe/ browser-reachable but SDK-free; caption table bundled — templated/no network), both golden snapshots (pace/+ingest/) byte-stable. NO git commit (operator commits between stories). epic-4 stays in-progress (4-1 is the first Epic-4 story, not the capstone).
  4-1: done
  # 4-2 moved backlog -> ready-for-dev (create-story). Pre-generated closing Saga (FR-10, NFR-5, Layer 2 — Told): the ONE LLM-authored prose passage in the project, displayed at the victory milestone with ZERO runtime LLM (offline-at-replay). Built + tested against a MOCKED @anthropic-ai/sdk — NO real claude-opus-4-8 call, no key in dev/CI; the single real Saga bake is the DEFERRED Epic-5 operator step (same established decision as Story 3.2). ADDS src/scribe/saga-author.ts (SagaAuthor: claude-opus-4-8 SINGLE PROSE call — NOT forced-tool, prose not structured — over the full NormalizedEvent[] window, INJECTABLE AnthropicLike client reused VERBATIM from 3.2's claude-interpreter.ts + lazy SDK import; the testable authoring logic lives in src/scribe/ NOT scripts/ because vitest only runs src/**/*.test.ts — the 3.2 precedent; browser-UNREACHABLE -> tree-shaken), src/scribe/saga.ts (the SDK-FREE browser-reachable READER readSaga(bundle)->string|null; reads ReplayBundle.saga [Story 1.2 field, z.string().nullable(), unchanged], handles the unauthored null fail-closed-to-default posture), scripts/scribe-saga.ts (THIN offline CLI mirroring scripts/interpret.ts — argv/fs glue + billed-call stderr notice + the deferred-bake header; the Story 5.2 bundle seam) + a package.json saga:story-10-1 jiti entrypoint (NOT in test/build/CI). WIRES the victory display additively: RenderPort.renderSaga?(saga) one-way command (the renderCaptions? precedent) fired ONCE by arena-boot at the BattleState.victory false->true latch edge on the forward tick (boot-owned sagaShown guard; seek/restart SNAP don't narrate the milestone) + a Phaser victory-panel draw. CRITICAL DATA-SOURCE REALITY: the boot derives from the COMMITTED FIXTURES via ?raw (NO ReplayBundle loaded in the browser yet — that's Story 5.2), so the dev/CI fixture path has no baked Saga (null); the gate proves the READER (given a bundle) + the WIRING (fires at the victory edge) + R4 + byte-stability; the live dev victory panel is dormant until Epic 5 bakes the Saga and 5.2 loads the bundle (the same dormant-in-fixture reality as summon@3.4). Optional DEV-only ?saga preview hook (import.meta.env.DEV, DCE'd in prod — the ?cinematic= precedent) to operator-verify the panel pre-bake. R4: saga.ts SDK-free + phaser-free (r1-discipline.test.ts EXTENDED to add 'saga.ts' to SCRIBE_MODULES); @anthropic-ai/sdk only in saga-author.ts + scripts/scribe-saga.ts (R4-allowed scribe/+scripts/); grep -ril anthropic dist/ = NOTHING (the real R4 proof). R1/Layer-2: the Saga makes no truth claim feeding mechanics; reading/displaying it mutates no Layer-0 state (one-way renderSaga? command, returns no BattleState). GATE-VERIFIABLE: mocked-client author (claude-opus-4-8 text request, Tolkien system prompt, event window, no tools, canned-text return, fail-loud-on-no-text-block, no-real-network grep); reader (verbatim baked Saga + null + byte-stable round-trip); the boot fires renderSaga? exactly once at victory NOT on seek/restart with no SDK on the path; R4 dist-grep + saga.ts source-grep; both golden snapshots byte-stable. OPERATOR-VERIFIED (NOT a gate): the on-screen victory Saga panel legibility/typography (jsdom advances no Phaser tweens). NOT the live Captions (FR-9, Story 4.1 done), NOT the full ReplayBundle assembly/loading (Story 5.2). Baseline 705 tests / 69 files at HEAD bd88db5; additive under src/scribe/ + scripts/ + a thin render/ seam. NO git commit (operator commits between stories). epic-4 stays in-progress (4-2 is not the first Epic-4 story, not the capstone).
  # 4-2 moved ready-for-dev -> in-progress (dev-story). Implementing the pre-generated closing Saga machinery (FR-10, NFR-5, Layer 2 — Told) against a MOCKED @anthropic-ai/sdk (no real claude-opus-4-8 call/key in dev/CI; the single real bake is the deferred Epic-5 operator step). Adding src/scribe/saga-author.ts (injectable SagaAuthor, single prose call), src/scribe/saga.ts (SDK-free reader), scripts/scribe-saga.ts (thin offline CLI) + saga:story-10-1 script; wiring RenderPort.renderSaga? + the arena-boot victory-edge fire + a Phaser victory panel. epic-4 stays in-progress.
  # 4-2 moved in-progress -> review (dev-story). DONE the closing-Saga machinery, red-green against the 4 committed ATDD files (saga-author.test/saga.test/arena-boot-saga.test/r1-discipline.test) + 1 added unit test, ZERO real network. ADDED src/scribe/saga-author.ts (SagaAuthor: claude-opus-4-8 SINGLE PROSE messages.create — no tools/tool_choice, no sampling params, max_tokens 2048 — over the full JSON-serialized NormalizedEvent[]; injectable AnthropicLike client reused VERBATIM from 3.2's claude-interpreter.ts + lazy SDK import; promptVersion config knob; fail-loud on no text block; testable logic in src/ NOT scripts/ per the vitest-include 3.2 precedent; browser-UNREACHABLE -> tree-shaken), src/scribe/saga.ts (SDK-free+phaser-free readSaga(bundle)->string|null; reads ReplayBundle.saga [Story 1.2 field unchanged]; null = not-yet-authored, fail-closed-to-default NOT an error), scripts/scribe-saga.ts (THIN offline CLI mirroring scripts/interpret.ts — argv/fs glue + billed-call stderr notice + deferred-bake header) + package.json saga:story-10-1 jiti entrypoint (NOT in test/build/CI). WIRED the victory display additively: RenderPort.renderSaga?(saga) one-way command (the renderCaptions? precedent) fired ONCE by arena-boot at the BattleState.victory false->true latch edge on the forward tick (boot-owned sagaShown guard; seek/restart SNAP don't narrate; null Saga = nothing shown) + a Phaser victory-panel add.text draw (arena-scene sagaPanel + phaser-render-adapter.renderSaga). BootDeps gained saga? (resolved-string override / test injection / dev preview, takes precedence) + bundle? (the Story-5.2 readSaga(bundle) path); the boot imports ../scribe/saga (readSaga) NOT saga-author (offline-at-replay, structural). DATA-SOURCE REALITY: no ReplayBundle in the dev/CI fixture path (Story 5.2 loads it), so the production Saga is null there and the live victory panel is DORMANT until Epic 5 bakes the Saga + 5.2 loads the bundle (same dormant-in-fixture reality as summon@3.4); the SAME renderSaga? wiring lights up unchanged when 5.2 lands. DEV-only ?saga preview hook in main.ts behind import.meta.env.DEV (the ?cinematic= DCE precedent) threads a canned dev Saga so the operator can WATCH the panel during pnpm dev — VERIFIED dead-code-eliminated from dist (its unique phrases absent from the prod bundle), injects no real bundle, does not alter the production path. R4: r1-discipline.test.ts EXTENDED ('saga.ts' added to SCRIBE_MODULES; saga-author.ts deliberately NOT — it is supposed to import the SDK); eslint.config.ts UNTOUCHED; grep -ril anthropic dist/ = NOTHING (the real R4 proof). R1/Layer-2: the Saga makes no truth claim feeding mechanics; reading/displaying it mutates no Layer-0 state (one-way renderSaga? returns void). GATE-PROVEN: mocked-client author (claude-opus-4-8 text request + Tolkien system prompt + event window + no tools + canned-text-trimmed return + fail-loud-on-no-text-block + no-real-network grep + model/promptVersion knobs); reader (verbatim baked Saga + null + byte-stable round-trip); the boot fires renderSaga? exactly once at the victory edge NOT on seek/restart/paused with no SDK on the path; R4 dist-grep + saga.ts source-grep. OPERATOR-VERIFIED (NOT a gate): on-screen victory Saga panel legibility/typography (jsdom advances no Phaser tweens). Gates: typecheck/lint clean (R1/R4/R5 zones UNCHANGED), 73 files / 739 tests (was 69/705 at HEAD bd88db5: +4 files [saga-author.test, saga.test, arena-boot-saga.test, saga-author.unit.test], +34 tests), build OK, no anthropic in dist, both golden snapshots (pace/+ingest/) byte-stable. Minor documented deviations: BootDeps carries BOTH saga? and bundle? (bundle? makes readSaga a genuine non-dead usage AND is the exact Story-5.2 seam); SagaAuthor adds a promptVersion knob + MAX_TOKENS 2048 (config-as-data). NOT git-committed (operator commits between stories). epic-4 stays in-progress.
  # 4-2 review -> done (senior-review fix round 1, 2026-06-15). Review APPROVED both ACs (gate-half); 4 low findings, no high/med. F1 (fix, the only code change): SagaAuthor.authorSaga let a present-but-empty text block ({type:'text', text:''}) pass the typeof-string find-guard, then '' === undefined was false, so it returned '' and baked an EMPTY Saga — violating AC1's "a refusal/empty must not silently bake an empty Saga". Fixed at root: trim first, then throw when the trimmed length is 0, so ONE guard now covers BOTH no-text-block AND empty/whitespace-only-block. Pinned by 2 new saga-author.unit.test.ts cases (empty string + whitespace-only both reject), each verified meaningful. F2 (consider, doc): documented the boot-owned sagaShown latch as once-per-session BY DESIGN, deliberately NOT reset on restart (mirroring captionHistory) — play->victory->restart->replay does not re-fire the panel; behavior intentionally unchanged (flipping to reset-on-restart is out of scope/behavior-changing per the synthesizer). F4 (doc): File List/Debug Log test count 739 -> 742 (verified actual). F3 (refuted, no code change): devMaxEpoch=-Infinity for an empty/all-NaN transcript is byte-identical to scripts/interpret.ts:49-51; the story MANDATES mirroring interpret.ts VERBATIM, so fixing it here would diverge the two CLIs and break that contract — operator-only path, unreachable on a real timestamped Story 10.1 session; the finding itself states "No change needed". Gates re-GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 73 files / 744 tests (was 742; +2 F1 guards), build OK, grep -ril anthropic dist/ = NOTHING, both golden snapshots (pace/+ingest/) byte-stable. NO git commit (operator commits between stories). epic-4 stays in-progress (4-2 is not the Epic-4 capstone).
  4-2: done
  # 4-3 moved backlog -> ready-for-dev (create-story). Always-on signature-beat TEACHING (FR-11, SM-1, Layer 2 — the teaching/transparency layer): a concise PLAIN-DEV one-liner AUTO-surfaces at each of the 3 signature beats (Shaman/Dispel/Summon) with NO viewer action, then AUTO-dismisses on its own. RESOLVED module home = src/portal/teaching.ts (architecture.md L378 maps FR-11 to portal/+render/; L363-364 names portal/ the transparency home; the on-demand Legend [4.4] also lives in portal/) — NOT scribe/ (scribe is the Tolkien voice; teaching is PLAIN-DEV, a distinct register that TEACHES the agentic-dev concept). ADDS src/config/teaching.json (config-as-data, keyed by BeatType -> ONE fixed plain-dev one-liner each, NO rotation — a stable lesson; mirrors captions.json) + src/portal/teaching-config.ts (.strict() exhaustive Zod loader w/ a TEACHING_MAX_LEN brevity bound, validate-at-load fail-closed; mirrors captions-config.ts/model-tuning.ts) + src/portal/teaching.ts (PURE planTeaching(prev,next,beatsAdvanced,view) -> TeachingOp[], MIRRORS planCaptions/planBeatBehaviors). TRIGGER = REUSE the beat-behavior.ts L1->L0 bridge keyed by beatType (NO new beat tag / Layer-0 path): dispel -> on a dispel-tagged beat firing; shaman -> on the breakthrough-discharge (isBreakthroughDischarge + hasShaman — the swarm-clear/death moment, AC1's worked example); summon -> on a summon-tagged breakthrough (dischargeThreshold from MODEL_TUNING, NO hardcode). TIMING = pure data decides WHAT + dwellMs (finite); the Phaser scene arms the auto-dismiss timer (this.time.delayedCall) — the BeatBehaviorIntent.durationMs split (time lives render-side). DISPLAY = additive one-way RenderPort.renderTeaching?(ops) (the renderCaptions?/renderSaga? precedent) + a distinct scene teaching banner (NON-overlapping the 4.1 caption band) + the boot threading planTeaching on the FORWARD TICK ONLY (seek/restart SNAP do not surface teaching; no new boot state — teaching is stateless per-transition). ACCURACY (grounded): each op carries the firing annotation's groundingPointer.eventRefs; every ref resolves to a real overlay event (no dangling). REACHABILITY: dispel@u-0002#1 + shaman@u-0010#0 fire on the COMMITTED fixture (the 3.5 win — auto-surface proven end-to-end for 2 of 3); summon UNIT-only (fixture omits it by design — the 3.3/3.4 honest gap; fires e2e w/ no code change once the bundle carries it, Epic 5). R1/Layer: teaching READS the frozen read-only AnnotatedView + Layer-0 deltas, writes NO mechanics field, returns no BattleState; portal/ has NO lint zone so the SDK-free + phaser-free SOURCE-GREP (mirroring scribe/r1-discipline.test.ts) is the real R4/R5 guard (lint's global anthropic+phaser bans also hold). GATE-VERIFIABLE: right one-liner per beat type; auto-surface fires on the beat (no viewer action); auto-dismiss dwell encoded (finite, scene timer); SM-C2 brevity (max-len bound + <=1 op/beat/transition); accuracy-to-event (grounded refs resolve). OPERATOR-VERIFIED (NOT a gate — pnpm dev; jsdom advances no timers/tweens): on-screen placement/legibility/timing FEEL. Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNCHANGED), full suite green (baseline 73 files / 744 tests at HEAD 97ffc3f), BOTH golden snapshots (pace/+ingest/) byte-stable (no Layer-0 code), build OK + grep -ril anthropic dist/ = NOTHING (teaching + config SDK-free). NOT the on-demand Legend/transparency portal overlay (that is Story 4.4). NO git commit (operator commits between stories). epic-4 stays in-progress (4-3 is not the first Epic-4 story, not the capstone).
  # 4-3 moved ready-for-dev -> in-progress (dev-story). Implementing the always-on signature-beat TEACHING (FR-11, SM-1): a PURE planTeaching planner in src/portal/teaching.ts (mirrors planCaptions/planBeatBehaviors) emitting a plain-dev one-liner TeachingOp per signature beatType firing (dispel on the dispel-tagged beat; shaman on the breakthrough-discharge death; summon UNIT-only — fixture omits it) keyed off the beat-behavior.ts L1->L0 bridge, + config-as-data src/config/teaching.json + a .strict() src/portal/teaching-config.ts loader w/ a TEACHING_MAX_LEN brevity bound, + the additive one-way RenderPort.renderTeaching? command + a distinct Phaser teaching band (delayedCall auto-dismiss) + the arena-boot forward-tick wire (seek/restart SNAP do not surface). epic-4 stays in-progress.
  # 4-3 moved in-progress -> review (dev-story). DONE the always-on signature-beat TEACHING (FR-11, SM-1), red-green against the 5 committed ATDD files (teaching.test/teaching.unit.test/r1-discipline/arena-boot-teaching/arena-teaching) + 1 added unit test, ZERO test edits (none encoded a wrong expectation). ADDED src/portal/teaching.ts (PURE planTeaching(prev,next,beatsAdvanced,view) -> TeachingOp[] — at most one plain-dev one-liner op per signature beatType firing, deduped by beatType for SM-C2; mirrors planCaptions/planBeatBehaviors; reuses the beat-behavior.ts L1->L0 bridge + isBreakthroughDischarge reading MODEL_TUNING.insight.dischargeThreshold, NO hardcode; carries the firing annotation's groundingPointer.eventRefs as the accuracy proof + a finite per-beatType dwellMs ~4000ms; PURE/deterministic/no-mutation; returns no BattleState, writes no mechanics field — R1 data-level), src/portal/teaching-config.ts (.strict() exhaustive Zod loader keyed by the 3 BeatType members, OneLinerSchema = z.string().min(1).max(TEACHING_MAX_LEN), TEACHING_MAX_LEN=140 the ONE SM-C2 brevity source of truth, validate-at-load fail-closed; mirrors captions-config.ts/model-tuning.ts), src/config/teaching.json (config-as-data, one FIXED plain-dev line per beat — shaman=root-cause, dispel=assumption-then-verify, summon=breakthrough-after-struggle; no rotation), src/portal/teaching-config.unit.test.ts (loader fail-closed: missing key/typo key/over-len/empty/bumped-version each throw). WIRED the display additively: RenderPort.renderTeaching?(ops) one-way OPTIONAL command (TeachingOp type-only; the renderCaptions?/renderSaga? precedent) + PhaserRenderAdapter.renderTeaching forwarding ready-guarded to the scene + ArenaScene.renderTeaching (create-once teachingText BOTTOM band at y~700, distinct from the y~40 caption band; sets+shows text, arms this.time.delayedCall(dwellMs, hide) auto-dismiss, a new op cancels/replaces the pending one; fail-closed never-throws) + arena-boot threading planTeaching + adapter.renderTeaching?.(...) on the FORWARD TICK ONLY (seek/restart/paused SNAP surface nothing; no new boot state — teaching is stateless per-transition). TRIGGER PARITY: dispel on the dispel-tagged Beat[0] (text===TEACHING.dispel, grounding ['u-0002#1','u-0002#2','u-0003#0']); shaman on the breakthrough-discharge death (text===TEACHING.shaman, grounding ['u-0009#0','u-0010#0']); summon UNIT-only (fixture omits it — the 3.3/3.4 honest gap; fires e2e with no code change once a summon-tagged annotation ships, Epic 5). REACHABILITY: dispel+shaman auto-surface live on the committed fixture (the 3.5 win — two of three beats). GATE-PROVEN: right one-liner per beat type; auto-surface on the beat with no viewer action (boot forward-tick, no toggle/open()); auto-dismiss encoded (finite dwellMs + scene timer); SM-C2 brevity (max-len bound + <=1 op/beat/transition); accuracy-to-event (grounding refs resolve, no dangling); R1/R4/R5 (SDK-free+phaser-free source-grep, no mechanics write, render->portal type-only). OPERATOR-VERIFIED (NOT gates — jsdom advances no Phaser timers/tweens): on-screen placement (non-overlapping band)/legibility/dwell timing FEEL. Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 79 files / 788 tests (was 73/744 at HEAD 97ffc3f: +6 files [5 ATDD + 1 added teaching-config.unit.test] / +44 tests), build OK, grep -ril anthropic dist/ = NOTHING (teaching+config templated/SDK-free), both golden snapshots (pace/+ingest/) byte-stable (no Layer-0 code). Minor documented deviation: 1 unit test ADDED on top of the ATDD suite (teaching-config.unit.test.ts — the loader fail-closed guard, the model-tuning.unit.test.ts precedent). NOT the on-demand Legend/transparency portal overlay (Story 4.4). NOT git-committed (operator commits between stories). epic-4 stays in-progress (4-3 is not the Epic-4 capstone).
  # 4-3 review -> done (senior-review fix round 1, 2026-06-15). Review APPROVED both ACs (gate-half); 0 high / 0 med / 7 low. The only code change is F3 (consider, render-display): ArenaScene.renderTeaching now renders the LAST op (last-op-wins) instead of iterating every op, so a valid multi-op transition (dispel+summon co-fire — teaching.unit.test.ts) no longer flashes the earlier line ~0ms or churns the dismiss timer; exactly one delayedCall is armed for the surviving line's full dwell. Pinned by a new RED-then-GREEN regression in arena-teaching.test.ts (spies the Scene clock: 1 call, last op's dwell). Stacking distinct beatType lines stays deferred to Epic 5; unreachable on the committed fixture (dispel@Beat0 and shaman@discharge never share a transition). F1 (doc count 785->788 / +41->+44) + F2 (last_updated monotonic, re-bumped to 09:00:00Z) re-verified. F4 (shaman+summon co-fire on a dual-tagged breakthrough — intentional, mirrors planBeatBehaviors dual-emit, no AC needs precedence), F5 (shaman keys on isBreakthroughDischarge+hasShaman not anchor-this-transition — BY DESIGN per Dev Notes #3/AC1, lesson lands on the felt death; negative test teaching.test.ts:156-169), F6 (groundingRefs aliases the frozen readonly eventRefs — compile-time readonly blocks mutation, render reads only text/dwellMs, mirrors beat-behavior.ts), F7 (transient __probe__.test.ts already removed — absent from the tree) all independently re-verified and REFUTED (no code change). Gates re-GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 79 files / 789 tests (was 788; +1 F3 regression), build OK, grep -ril anthropic dist/ = NOTHING, both golden snapshots (pace/+ingest/) byte-stable. NO git commit (operator commits between stories). epic-4 stays in-progress (4-3 is not the Epic-4 capstone).
  4-3: done
  # 4-4 moved backlog -> ready-for-dev (create-story). The FINAL Epic-4 story (the capstone): the ON-DEMAND
  # Legend / transparency portal (FR-11, UJ-2) — a viewer-OPENED fantasy<->real overlay grounded in the real
  # events, distinct from the ALWAYS-ON teaching one-liners (4.3, done). ADDS src/portal/portal.ts (the PURE,
  # phaser-free transparency core: LEGEND_BEATS/LEGEND_ACTIONS coverage domains + getLegendEntries() + the PURE
  # resolveGrounding(annotation, view) that maps an active beat's groundingPointer.eventRefs to the REAL Layer-0
  # NormalizedEvent(s) it dramatizes via the Story-3.1 read-only AnnotatedView — fantasy -> real, accurate to the
  # real Event, FAIL-LOUD on a dangling ref [the frozen-overlay-invariant posture, the 3.1/3.2 no-dangling
  # build-time guarantee]) + src/portal/legend-config.ts + src/config/legend.json (config-as-data, .strict() Zod
  # loader mirroring teaching-config.ts; the 3 beats {shaman,dispel,summon} + the 4 CORE actions
  # {melee=strike/edit, spell=spell/test, scout=scout/read, aetherStorm=Aether-Storm/rate-limit} authored
  # ACCURATE to translation-rules.json; LEGEND_MAX_LEN brevity bound closes the rubric's unbounded-"concise"
  # finding) + a THIN render overlay src/render/legend-overlay.ts (a DOM overlay, the controls.ts precedent —
  # NO phaser, jsdom-testable; open/close/toggle/isOpen/destroy via one AbortController). OPEN/CLOSE WITHOUT
  # INTERRUPTING PLAYBACK (the load-bearing decision): the overlay toggles ONLY its OWN visibility — it
  # dispatches NO PlaybackAction, NEVER sets cinematicActive (the 3.4 suspend path WOULD interrupt — deliberately
  # NOT reused), NEVER touches the reducer/cursor/BattleState; the status-gated rAF loop keeps ticking unchanged.
  # Reuses the 2.2 reducer / 2.5 controls ONE-WAY posture (render reads, reducer owns state), NOT the cinematic
  # guard. Wired additively into arena-boot (mount alongside createControls; `legend` on ArenaHandle; torn down
  # in destroy()); main.ts mounts the toggle + optional ?legend DEV auto-open (the ?cinematic=/?saga DCE
  # precedent). GATE-VERIFIABLE: Legend covers the 3 beats + 4 core actions; resolveGrounding maps to the CORRECT
  # real eventIds end-to-end on the committed fixture (dispel@u-0002#1 + shaman@u-0010#0; summon unit-only — the
  # 3.3/3.4 honest gap) + fail-loud on a fabricated dangling ref + determinism + no-input-mutation; open/close
  # leaves PlaybackState (cursor/status/speed) AND BattleState DEEP-EQUAL (the non-interruption proof) + never
  # dispatched + isCinematicActive stays false + the loop still advances while open; R1/R4/R5 (portal SDK-free +
  # phaser-free source-grep [r1-discipline.test.ts extended with portal.ts + legend-config.ts], no mechanics
  # write, render->portal type-only). OPERATOR-VERIFIED (NOT a gate — pnpm dev; jsdom advances no Phaser tweens /
  # lays out no real pixels): the overlay layout / legibility / open-close FEEL. Gates: typecheck/lint clean
  # (R1/R4/R5 hold, eslint.config.ts UNCHANGED), full suite green (baseline 79 files / ~814 cases at HEAD
  # 6cfcdb5), BOTH golden snapshots (pace/+ingest/) byte-stable (no Layer-0 code), build OK + grep -ril anthropic
  # dist/ = NOTHING (Legend is templated config-as-data, no LLM/network). NOTE: 4-4 is the Epic-4 CAPSTONE — per
  # the explicit instruction, after 4-4 reaches done (dev-story -> review -> done) epic-4 ALSO flips
  # in-progress -> done. create-story leaves 4-4 ready-for-dev + epic-4 in-progress. NO git commit (operator
  # commits between stories). epic-4 stays in-progress.
  # 4-4 moved ready-for-dev -> in-progress -> review (dev-story). The FINAL Epic-4 story (the capstone):
  # the ON-DEMAND Legend / transparency portal (FR-11, UJ-2) — a viewer-OPENED fantasy<->real overlay
  # grounded in the real events, distinct from the ALWAYS-ON teaching one-liners (4.3, done). Made the 5
  # committed ATDD red files green HONESTLY (none deleted/skipped/weakened) + fixed 2 ATDD-scaffold
  # lint/typecheck failures (genuinely-dead unused imports/helpers the red files copied verbatim but never
  # referenced — pace/translate in portal.test, the full runIngest/timeline()/ingest chain in
  # arena-boot-legend.test; each removal annotated in-file, no assertion weakened). ADDED src/portal/portal.ts
  # (the PURE phaser-free core: LEGEND_BEATS/LEGEND_ACTIONS coverage domains + getLegendEntries() + the PURE
  # resolveGrounding(annotation, view) mapping an active beat's groundingPointer.eventRefs to the REAL
  # Layer-0 NormalizedEvent(s) via the 3.1 read-only AnnotatedView — fantasy -> real, accurate, FAIL-LOUD on
  # a dangling ref [the frozen-overlay-invariant posture] + resolveActiveBeatGrounding helper), src/portal/
  # legend-config.ts (.strict() exhaustive Zod loader, LEGEND_MAX_LEN=160 brevity bound, validate-at-load
  # fail-closed; mirrors teaching-config.ts) + src/config/legend.json (config-as-data, the 3 beats + 4 CORE
  # actions {melee=strike/edit, spell=spell/test, scout=scout/read, aetherStorm=Aether-Storm/rate-limit}
  # authored ACCURATE to translation-rules.json), src/render/legend-overlay.ts (a THIN DOM overlay, the
  # controls.ts precedent — NO phaser, jsdom-testable; open/close/toggle/isOpen/destroy via one
  # AbortController). NON-INTERRUPTION (the load-bearing decision): the overlay toggles ONLY its OWN
  # visibility — dispatches NO PlaybackAction, NEVER sets cinematicActive (the 3.4 suspend path WOULD
  # interrupt — deliberately NOT reused), NEVER touches the reducer/cursor/BattleState; the status-gated rAF
  # loop keeps ticking unchanged. Wired additively into arena-boot (mount alongside createControls; `legend`
  # on ArenaHandle; torn down in destroy()); main.ts adds the always-visible toggle + DEV-only ?legend
  # auto-open (DCE'd from prod, the ?cinematic=/?saga precedent). DISPLAY-SEAM DECISION: kept the ENTIRE
  # Legend in the DOM overlay — did NOT add a RenderPort.renderLegendGrounding? Phaser command (no Phaser
  # reveal required), so render-port.ts + the scene are UNTOUCHED. GATE-PROVEN (Vitest): AC1 coverage (3
  # beats + 4 core actions), AC2 grounding (dispel -> u-0002#1/u-0002#2/u-0003#0 + shaman -> u-0009#0/u-0010#0
  # end-to-end on the committed fixture, IN ORDER, same object identities; fabricated dangling ref throws;
  # deterministic + no-input-mutation; summon symmetry unit-only — the 3.3/3.4 honest gap), AC1
  # non-interruption (PlaybackState cursor/status/speed + BattleState DEEP-EQUAL across open->close, paused +
  # mid-playing; no render command driven; isCinematicActive stays false; loop still advances while open),
  # R1/R4/R5 (portal SDK-free + phaser-free source-grep; no mechanics write; render->portal type-only).
  # OPERATOR-VERIFIED (NOT a gate — pnpm dev; jsdom advances no Phaser tweens / lays out no real pixels): the
  # overlay layout / legibility / open-close FEEL. Gates: typecheck/lint clean (R1/R4/R5 hold,
  # eslint.config.ts UNTOUCHED), 83 files / 842 tests (was 78 files/789 passing at the RED baseline; +5 ATDD
  # files now green), build OK, grep -ril anthropic dist/ = NOTHING (Legend is templated config-as-data, no
  # LLM/network; ?legend DEV flag DCE'd from prod), BOTH golden snapshots (pace/+ingest/) byte-stable (no
  # Layer-0 code). NO git commit (operator commits between stories). NOTE: 4-4 is the Epic-4 CAPSTONE — per
  # the project convention (dev-story flips to review, senior-review flips to done) the 4-4 -> done AND
  # epic-4 in-progress -> done flips are the senior-review step (the story Dev Notes states epic-4 flips
  # "after 4-4 reaches done (dev-story -> review -> done)"). epic-4 stays in-progress.
  # 4-4 review -> done (senior-review fix round 1, 2026-06-15). Review APPROVE: 5 low-severity findings, no
  # high/medium. LOW-001 (the only `fix`) confirmed already-applied + accurate — the GREEN doc count is
  # 83 files / 842 cases (re-measured via pnpm test this round; the stale 833 in this comment block + the
  # story Debug Log corrected to 842). LOW-002..005 REFUTED with independent verification (no source change —
  # documented invariants / double-guarded): LOW-002 resolveActiveBeatGrounding is exported+unit-tested but
  # has no prod caller and the boot's latest-reached-beat selection is deliberately commented (one annotation
  # per beatType in the fixture, fail-loud unreachable under the frozen-overlay invariant); LOW-003 the
  # readonly[] annotation does not type-enforce exhaustiveness (tsc probe: dropped member compiles, typo
  # errors TS2820) but coverage is double-pinned by portal.test runtime asserts + the .strict() Zod schema;
  # LOW-004 active-beat grounding is intentional open-time-snapshot semantics (operator-feel, no AC needs a
  # live tracker); LOW-005 the dual LegendEntry is deliberate in-file-documented renderer<->portal decoupling
  # (narrow->string assignable, structural compat at the boot seam). Gates re-run GREEN: typecheck 0, lint 0
  # (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 83 files / 842 cases, build OK, grep -ril anthropic dist/
  # empty, both golden snapshots byte-stable. NO git commit. epic-4 stays in-progress (the operator flips
  # epic-4 -> done as the capstone, per the Epic-1/2/3 flip-post-senior-review precedent).
  4-4: done
  # Epic 5 — Showcase Delivery (Scrub -> Bundle -> Public URL)
  # 5-1 moved backlog -> ready-for-dev (create-story). FIRST Epic-5 source story (the PRIVACY GUARDRAIL,
  # architecture.md#Privacy&Security): a config-driven secret/PII scrub over the ingested Session + the
  # PUBLISH GATE that Story 5.2's bundle build enforces. Since the public ReplayBundle carries real Story
  # 10.1 transcript data and the fantasy/narration layers do NOT redact, this gates publishing. ADDS (all
  # NEW/additive, no existing source edited): src/config/scrub-patterns.json (versioned deny-pattern set,
  # config-as-data NFR-4: secret/token/credential/pii-email/home-path/db-role categories), src/scrub/
  # scrub-patterns.ts (.strict() Zod loader compiling patterns->RegExp, fail-closed at load — the
  # teaching-config/captions-config/model-tuning precedent), src/scrub/scrub.ts (PURE deterministic
  # scrubSession(events)->{scrubbedEvents, report, scrubHash}: walks payload string leaves, replaces
  # matches with «REDACTED:{category}» tokens, emits a manual-review report of redactions BY
  # CATEGORY/COUNT + locations {eventId,jsonPath} + suspicious-unmatched candidates — NEVER a secret
  # value; scrubHash=sha256(canonicalJSON(scrubbedEvents)+patternSetVersion) REUSING freeze.ts's
  # canonicalJSON, node:crypto only), src/scrub/gate.ts (PURE isPublishable({scrubResult,approval})->{ok,
  # reasons} + the ScrubApproval marker contract binding an approval to a specific scrubHash+reportHash —
  # BLOCKS on null/stale/tampered/mismatched approval, PASSES only on a matching valid marker; the
  # Story-5.2 wiring seam), scripts/scrub.ts (THIN offline argv/fs glue mirroring interpret.ts/scribe-saga.ts;
  # untested — vitest include is src/**/*.test.ts so logic lives in src/scrub/, the 3.2 split), a
  # planted-secret fixture + co-located scrub.test/gate.test/scrub-patterns.unit.test/r4-isolation.test +
  # a package.json scrub:story-10-1 script (NOT in test/build/CI). MODULE-HOME decision: NEW src/scrub/
  # (architecture names only scripts/scrub.ts, but vitest only runs src/**/*.test.ts -> testable logic must
  # live in src/, the Story-3.2 thin-script/src-logic precedent); no eslint zone — global anthropic/phaser
  # bans + the r4-isolation source-grep are the R4/R5 guard (eslint.config.ts UNTOUCHED). REPORT shape
  # resolved on documented merits: counts-by-category + locations + candidates, NEVER values (the
  # load-bearing privacy test: serialize report+scrubbed events, assert .not.toContain each planted
  # secret). GATE contract resolved: pure clock-free predicate; the marker content-addresses the scrubbed
  # output+report so a stale approval can't ship a changed session; ReplayBundle schema NOT edited (5.2
  # adds bundle fields, the TuningConfigSchema forward-reference precedent). DATA-SOURCE REALITY: the FULL
  # real session .sources/story-10-1/* (810 lines) is git-ignored/not in CI; the committed fixture (1.3) is
  # the hand-redacted thin slice with NO planted secrets -> the GATE is proven against a NEW planted-secret
  # fixture (the 3.2/3.4 honest-gap posture), and the real .sources scrub + the human review/approval are
  # the DEFERRED operator step (the gate only enforces a valid marker EXISTS, it cannot prove a human read
  # the report). GATE-VERIFIABLE: planted patterns redacted; report lists them by category/count with NO
  # value leaked; the gate BLOCKS an unscrubbed/unreviewed bundle + PASSES a scrubbed+reviewed one; R4
  # scrub SDK-free; determinism (same input -> same scrubbed output + report). PURE (R2-adjacent: no
  # clock/RNG/network/IO in src/scrub/; fs lives in scripts/ glue). Gates: typecheck/lint clean (R1/R4/R5
  # hold, eslint.config.ts UNCHANGED), full suite green (baseline 83 files at HEAD 6025447), build OK +
  # grep -ril anthropic dist/ = NOTHING (src/scrub/ is browser-unreachable, tree-shaken), BOTH golden
  # snapshots (pace/+ingest/) byte-stable (no Layer-0 code). NOT the bundle build (5.2), final art (5.3),
  # or deploy (5.4). NO git commit (operator commits between stories). epic-5 stays in-progress (5-1 is the
  # first Epic-5 story; epic-5 already in-progress, no epic flip triggered).
  # 5-1 moved ready-for-dev -> in-progress -> review (dev-story). The PRIVACY GUARDRAIL: src/config/
  # scrub-patterns.json (.strict() loader) + src/scrub/{scrub-patterns,scrub,gate}.ts (PURE config-driven
  # redactor + manual-review report [category/count + locations + opaque-token candidates, NO value leaked] +
  # the publish-gate predicate isPublishable bound to scrubHash+reportHash that Story 5.2 wires) + thin
  # scripts/scrub.ts glue + a planted-secret fixture. Made the 4 ATDD red suites GREEN honestly (none
  # weakened); +37 tests (83->87 files, 842->879 tests). Gates GREEN: typecheck/lint clean (eslint.config.ts
  # UNCHANGED, R1/R4/R5 hold), build OK + grep -ril anthropic dist/ = NOTHING (src/scrub/ browser-unreachable,
  # tree-shaken), BOTH golden snapshots (pace/+ingest/) byte-stable (no Layer-0 code). The real scrub over the
  # git-ignored .sources/story-10-1/* + the human approval marker are the deferred operator step (Story 5.2
  # wires the gate). NO git commit. epic-5 stays in-progress (5-1 is not the Epic-5 capstone).
  # 5-1 review -> done (senior-review fix round 1, 2026-06-15). Approve-with-minor-fixes: 0 high / 2 med / 5
  # low; all fix/consider findings addressed, R8/R9 refuted. R1 (med): redactions sort now CODEPOINT not
  # localeCompare, so reportHash is host-independent (closes a cross-host approval-BLOCK footgun; no observable
  # change for the 6 ASCII categories). R2 (med, consider->fixed): the gate self-validates the result —
  # recomputes scrubHash from scrubbedEvents+report.patternSetVersion and BLOCKS on mismatch BEFORE trusting
  # the approval (defense-in-depth vs a 5.2 caller hand-mutating scrubbedEvents while reusing the stale field).
  # R3 (low): did NOT tighten password=\S+ (tightening was VERIFIED to leak the conn-string @host:port/db tail
  # because password-assignment pre-empts connection-string-credential) -> documented the greedy fail-safe +
  # added a password-literal-free conn-string fixture/test so connection-string-credential is exercised. R4
  # (low): documented db-role as intentionally-aggressive fail-safe operator-review noise (narrowing would risk
  # UNDER-redaction). R5 (low): home-path patterns now consume the path tail (/home/[^\s"']+ etc.) so
  # .ssh/id_rsa residual signal is also redacted (strictly more private). R6 (low): walkValue preserves input
  # key order (scrubHash stable via canonicalJSON re-sort). R7 (low): doc counts corrected. Behavioral fixes
  # each gained a test; +4 tests. Golden inline snapshot re-baselined for R5/R6 (no value leak). Gates GREEN:
  # typecheck/lint clean (eslint.config.ts UNTOUCHED, R1/R4/R5 hold), 87 files / 887 tests, build OK + grep -ril
  # anthropic dist/ = NOTHING, BOTH golden snapshots (pace/+ingest/) byte-stable. NO git commit. epic-5 stays
  # in-progress (5-1 is not the Epic-5 capstone).
  epic-5: in-progress
  5-1: done
  # 5-2 moved backlog -> ready-for-dev (create-story). The FINAL Epic-5 CODE story (NFR-5): the offline
  # ReplayBundle build orchestration + the browser bundle-LOAD path. (1) NEW src/bundle/assemble-bundle.ts
  # — a PURE assembler (vitest only runs src/**/*.test.ts, so logic lives in src/, the 3.2/5.1 thin-script
  # split): assembleBundle({scrubResult, approval, annotations, versions, battleTimeline, tuningConfig,
  # saga, assetManifest}) runs the Story-5.1 isPublishable() gate FIRST (throws fail-closed on !ok), uses
  # scrubResult.scrubbedEvents as the PUBLIC normalizedEvents, freezeAnnotations() (3.2) to validate+
  # content-address+reject dangling refs, composes a ReplayBundleSchema-valid bundle, + bundleHash(bundle)
  # =sha256(canonicalJSON(bundle)) REUSING freeze.ts's canonicalJSON (AC2 whole-bundle determinism, distinct
  # from the embedded annotationHash run-identity key). (2) EDIT src/schema/replay-bundle.ts: ADD a nullable
  # `scrub: {scrubHash, reportHash, patternSetVersion, approval: ScrubApprovalSchema}|null` — the Story-5.1
  # forward-reference (the TuningConfigSchema precedent); schemaVersion STAYS literal(1) (additive nullable =
  # backward-compatible). (3) REWRITE scripts/build-bundle.ts (currently a node -e placeholder) — thin argv/
  # fs glue mirroring scrub.ts/interpret.ts/scribe-saga.ts: ingest(1.3) -> scrub(5.1 scrubSession) -> load
  # ScrubApproval marker -> pace(translate(scrubbedEvents)) [baked timeline; scrubbing is mechanically inert
  # on pacing — string leaves only] -> interpret [MOCKED: FixtureInterpreter; --real selects ClaudeInterpreter]
  # -> saga [MOCKED: committed placeholder string; --real selects SagaAuthor] -> assembleBundle -> write
  # public/bundles/story-10-1.json. (4) EDIT src/render/arena-boot.ts + src/main.ts: the boot now LOADS
  # public/bundles/story-10-1.json (async fetch + ReplayBundleSchema.parse at the boundary) and runs from it
  # (bootFromBundle taking timeline/events/annotations/saga FROM the bundle), REMOVING the ?raw fixture
  # derivation + deriveTimeline() (offline-at-replay, NFR-5; the Story-4.2-prewired deps.bundle path
  # completed). (5) COMMIT a fixture-derived public/bundles/story-10-1.json (FixtureInterpreter annotations +
  # placeholder Saga + scrubbed fixture events + a dev ScrubApproval marker for a PASSING gate path) so the
  # app runs end-to-end now. Established MOCKED-LLM decision: NO real claude-sonnet-4-6/opus call, NO key in
  # dev/CI — the real full-session bake (real LLM + scrubbed .sources) is the DEFERRED operator step (the
  # --real CLI seam), regenerating the bundle with no code change. GATE-VERIFIABLE: assembler builds a Zod-
  # valid bundle (mocked); gate BLOCKS unscrubbed/unreviewed + PASSES scrubbed+approved; boot loads the
  # committed bundle + the Replay runs (headless boot smoke FROM the bundle); hash determinism (same inputs ->
  # same annotationHash + bundleHash); R4 (no anthropic in dist; assembler+boot SDK-free). OPERATOR-VERIFIED
  # (NOT a gate): the on-screen end-to-end replay after the real bake. Gates: typecheck/lint clean (R1/R4/R5
  # hold, eslint.config.ts UNTOUCHED), full suite green, BOTH golden snapshots (pace/+ingest/) byte-stable (no
  # Layer-0 code added), build OK + grep -ril anthropic dist/ = NOTHING. NOT final art (5.3) or deploy (5.4).
  # NO git commit (operator commits between stories; the deliverable public/bundles/story-10-1.json is staged
  # for the operator). epic-5 stays in-progress (5-2 is not the first Epic-5 story).
  # 5-2 moved ready-for-dev -> in-progress -> review (dev-story, baseline f5b5280). The offline ReplayBundle
  # build orchestration + the browser bundle-LOAD path are DONE (mocked LLM): (1) NEW src/bundle/assemble-
  # bundle.ts — PURE assembleBundle() runs the Story-5.1 isPublishable() gate FIRST (throws fail-closed),
  # uses scrubResult.scrubbedEvents as the public normalizedEvents, freezeAnnotations() (3.2), composes a
  # ReplayBundleSchema-valid bundle + bundleHash()=sha256(canonicalJSON) REUSING freeze.ts (AC2). (2) EDIT
  # src/schema/replay-bundle.ts — ADDED nullable+optional `scrub: ScrubProvenanceSchema|null` (the 5.1
  # forward-reference; schemaVersion STAYS literal(1)); ScrubApprovalSchema moved to a NEW crypto-free leaf
  # src/scrub/approval.ts (gate.ts re-exports) so the browser-reachable schema does NOT pull gate.ts's
  # node:crypto into the Vite bundle. (3) REWROTE scripts/build-bundle.ts — thin glue: ingest -> scrub ->
  # load ScrubApproval -> pace(translate(scrubbedEvents)) -> interpret [FixtureInterpreter; --real seam
  # = ClaudeInterpreter] -> saga [placeholder; --real = SagaAuthor] -> assembleBundle -> write. (4) EDIT
  # arena-boot.ts + main.ts — boot now LOADS public/bundles/story-10-1.json (loadBundle: fetch + Zod-parse)
  # and runs from it (bootFromBundle); REMOVED the ?raw/deriveTimeline derivation (offline-at-replay). (5)
  # COMMITTED public/bundles/story-10-1.json (FixtureInterpreter annotations + placeholder Saga + scrubbed
  # fixture events + a dev ScrubApproval marker for a PASSING gate). GATES GREEN: typecheck/lint clean
  # (R1/R4/R5 hold, eslint.config.ts UNTOUCHED), 92 files / 929 tests, BOTH golden snapshots (pace/+ingest/)
  # byte-stable, build OK + grep -ril anthropic dist/ = NOTHING, determinism = two builds byte-identical.
  # OPERATOR-VERIFIED (NOT a gate): the on-screen end-to-end replay after the real bake (--real). NO git
  # commit (the deliverable public/bundles/story-10-1.json is staged for the operator). epic-5 stays
  # in-progress (5-2 is not the first Epic-5 story; 5-3 art + 5-4 deploy remain).
  # 5-2 review -> done (senior-review fix round 1). Review APPROVED all 7 ACs; 0 high / 1 med / 4 low +
  # 1 (F6) — 4 actionable (F1 fix + F2/F3/F4), 2 confirmed-refuted (F5/F6). F1 (med): main.ts .catch now
  # surfaces a CLEAN observable failure (loud console.error + a role="alert" boot-error banner via
  # renderBootError) instead of re-throwing into a dangling unhandled rejection; first main.ts test
  # coverage (src/main.test.ts, jsdom). F2: loadBundle guards res.ok before res.json() (a missing bundle
  # on a static SPA-fallback host gives a clear HTTP error, not "Unexpected token <") + a 404 test. F3:
  # assembleBundle validates every baked-timeline beat sourceEventId resolves to a shipped event (defense-
  # in-depth at the sole composition point, mirroring freezeAnnotations) + 2 tests. F4: the mocked path
  # pins interpreterVersion/promptVersion to the fixture-v1 constants (the flags are --real-only) so a
  # stray --interpreter-version can't desync annotationHash from the embedded annotations. F5 (refuted):
  # bundleHash folding scrub.approval is by-design provenance (Decision §3); AC2 holds (approval is an
  # input; byte-identical two-build diff). F6 (refuted): scrub .nullable().optional() is intentional 1.2
  # backward-compat — the assembler ALWAYS populates scrub + the committed-bundle guard asserts non-null.
  # The committed public/bundles/story-10-1.json is BYTE-IDENTICAL after a rebuild (determinism intact).
  # Gates re-run GREEN: typecheck/lint clean (R1/R4/R5 untouched, eslint.config.ts UNCHANGED), 93 files /
  # 937 tests (was 92/931: +1 file main.test.ts, +6 tests), build OK + grep -ril anthropic dist/ = NOTHING,
  # BOTH golden snapshots (pace/+ingest/) byte-stable. NO git commit. epic-5 stays in-progress (5-3 art +
  # 5-4 deploy remain — operator/external steps).
  5-2: done
  5-3: backlog
  5-4: backlog
  # 5-5: payload-free bundle (abstracted grounding) — privacy refinement surfaced during the build
  # 5-5 moved backlog -> ready-for-dev (create-story). BUILD-SURFACED privacy/teaching refinement of Story
  # 5.2's bundle (NOT in epics.md; Given/When/Then ACs authored verbatim with the operator). HARD operator
  # line: the PUBLIC bundle must NEVER ship a real file/symbol name or path — only a mechanically-derived
  # COARSE ROLE. REPLACES the bundle's full normalizedEvents (today: payloads w/ raw content up to ~28KB/
  # event + real file paths like /work/project/src/schema/normalized-event.ts — verified) with a minimal
  # payload-free projectedEvents array keeping ONLY {orderKey+eventId (opaque), eventType, toolName,
  # outcome(isError?'isError':'success'), abstracted role}. ADDS a PURE name-discarding classifyRole(path)
  # (src/bundle/classify-role.ts; ordered first-match: test/schema/migration/config/doc/source; reads the
  # path at BAKE time, emits ONLY the token, discards the path — NO LLM/semantic mapping) + projectEvents
  # (src/bundle/project-events.ts). The assembler still freezes + hashes over the FULL scrubbed events
  # (annotationHash UNCHANGED — provenance is over the INPUT; the bundle carries the HASH not the events),
  # then ships ONLY the projection (full content + real names DROPPED). The portal grounding (4.4) is
  # re-pointed to the ABSTRACTED projection (tool + role + outcome + teaching concept from teaching.json,
  # NOT raw events/names); the verbatim 'sight-beyond-sight' deep-dive is a DEFERRED local-only mode (NOT
  # built — keep the seam clean). The scrub (5.1) still runs on the projection (defense-in-depth). The
  # change is mechanically inert at replay (the timeline is BAKED; no browser-reachable code reads
  # event.payload/toolName — verified; the projection keeps the eventIds the freeze/timeline/portal refs
  # resolve on). schemaVersion stays z.literal(1) (pre-ship v0.1; the only consumers are in-repo + updated
  # here). Regenerates the committed public/bundles/story-10-1.json (payload-free) + rewrites
  # committed-bundle.test.ts (byte-absence: grep-absent of raw content + paths/names + bounded per-event
  # size). GATE-VERIFIABLE (~fully): classifier purity+no-echo (AC3); projection 5-key shape + no-leak
  # .not.toContain (AC1); bundle byte-absence + bounded size (AC2); portal abstracted-grounding rows
  # accurate on the fixture (dispel+shaman) + fail-loud-on-dangling + no name/content (AC4); bake reads
  # full session but ships only the projection + scrub runs on it (AC5); R1/R4/R5; grep -ril anthropic
  # dist/ = NOTHING; BOTH golden snapshots (pace/+ingest/) byte-stable (NO Layer-0 code); rebuild
  # byte-identical. OPERATOR-VERIFIED (NOT a gate): on-screen abstracted-grounding legibility (pnpm dev).
  # NOT final art (5.3) or deploy (5.4). NO git commit (operator commits between stories). epic-5 stays
  # in-progress (5-5 is not the first Epic-5 story; no epic flip triggered).
  # 5-5 moved ready-for-dev -> in-progress -> review (dev-story, baseline 13a5137). Implemented the
  # payload-free bundle + abstracted grounding: pure name-discarding classifyRole (src/bundle/
  # classify-role.ts) + pure payload-free projectEvents (src/bundle/project-events.ts); replaced the
  # bundle's full normalizedEvents with a {orderKey,eventId,eventType,toolName,outcome,role}
  # projectedEvents (.strict() ProjectedEventSchema; schemaVersion stays 1). The assembler projects AFTER
  # freeze + ships the projection (annotationHash/freeze UNCHANGED over the full scrubbed events —
  # provenance over the INPUT; F3 dangling-beat guard re-pointed to projectedEvents + a §5 referenced-ids
  # fail-loud added). The portal grounds beats to abstracted {tool,role,outcome,concept} rows
  # (resolveAbstractedGrounding; concept reused from teaching.json) — AnnotatedView/applyOverlay made
  # GENERIC in E to minimize churn (§6 seam, documented); legend-overlay displays the abstracted rows (no
  # eventId/file/symbol name shown). Regenerated public/bundles/story-10-1.json (payload-free, byte-stable;
  # annotationHash + scrubHash IDENTICAL to the prior committed bundle — provenance unchanged). Made the 6
  # ATDD red tests green honestly (2 fixed WITH documented justification, none weakened) + re-pointed the
  # 1.2/5.2/4.4 bundle/portal/overlay/boot suites (incl. the legend/boot AC2 reveal now asserts the AC4
  # abstracted grounding instead of raw eventIds). Gates GREEN: typecheck/lint clean (eslint.config.ts
  # UNTOUCHED, R1/R4/R5 hold), 100 files / 1019 tests (was 93/937), build OK, grep -ril anthropic dist/
  # empty, dist bundle payload-free, BOTH golden snapshots (pace/+ingest/) byte-stable (Layer-0 untouched),
  # 3 rebuilds byte-identical (per-event <=203 bytes). Deferred 'sight-beyond-sight' deep-dive NOT built.
  # NO git commit (the regenerated bundle is staged for the operator). epic-5 stays in-progress (5-3 art +
  # 5-4 deploy remain — operator/external steps).
  # 5-5 review -> done (senior-review fix round 1). Review APPROVED all 5 ACs; 3 low findings, all resolved.
  # F1 (fix): the classify-role dotted-config-basename rule (tsconfig|eslint|vite|package)[^/]*$ over-matched
  # SOURCE files merely STARTING with a prefix (PackageList.tsx/vite-plugin-custom.ts/eslint-runner.ts/
  # packageResolver.ts -> 'config' not 'source' — a name-free but WRONG coarse role rendered to the viewer,
  # AC4 accuracy; NOT a leak). Fixed at root cause: (^|/)(tsconfig|eslint|vite|jest)\.[^/]+$ (dot required) +
  # (^|/)package\.json$ (exact); every legit config path still classifies 'config' via the .json/.config.*
  # rules, so the committed bundle's roles are UNCHANGED. Added F1/ANCHORING regression cases. F2 (consider,
  # applied): added .strict() to ReplayBundleSchema so a regression re-introducing a top-level normalizedEvents
  # (old leak vector) fails LOUD at the parse boundary instead of being silently stripped (defense-in-depth,
  # mirrors the per-event .strict(); verified the sole composition point + committed bundle carry exactly the
  # 9 declared keys) + 2 reject tests. F3 (likely-refute): REFUTED, no code change — eventId is the documented
  # §1 ref-stable opaque id (u-0001/u-0002#1; no name/path/content) every timeline/annotation ref + freeze
  # guard + portal lookup keys on; 6-key set matches EXPECTED_KEYS. Gates re-run GREEN: typecheck/lint clean
  # (eslint.config.ts UNTOUCHED, R1/R4/R5 hold), 100 files / 1024 tests (was 1019; +5 regression tests), build
  # OK, grep -ril anthropic dist/ empty, dist bundle payload-free, BOTH golden snapshots byte-stable,
  # public/bundles/story-10-1.json regenerated byte-IDENTICAL (annotationHash c10c15aa.., bundleHash a0ebc77c..
  # unchanged). NO git commit. epic-5 stays in-progress (5-3 art + 5-4 deploy remain).
  5-5: done
  # 5-6: reduced tagging-view + multi-stream merge (enables the full-session real bake)
  # 5-6 moved backlog -> ready-for-dev (create-story). BUILD-SURFACED enablement of the deferred
  # full-session real bake (NOT in epics.md; ACs authored verbatim from the operator's createBrief). The
  # ~689-event real Story 10.1 session (~294K tokens of full payloads) EXCEEDS the ~200K window for a
  # one-shot `claude -p interpret`. Two pieces: (1) a PURE reduced "tagging view" (NEW src/bundle/
  # tagging-view.ts) mapping the full NormalizedEvent[] -> a compact per-event {eventId, toolName, role
  # (reuse 5.5 classifyRole), outcome (isError), bounded scrub-safe snippet <=~200 chars} for the
  # interpreter/Saga PROMPT INPUT — PRESERVES eventId so groundings resolve, no payload, targets <250KB /
  # <~60K tokens (AC1/AC4); (2) MULTI-STREAM ingest in scripts/build-bundle.ts — accept repeated
  # --transcript/--stream-id pairs OR a --session-manifest JSON + --journal, parse+normalize EACH with its
  # stream-id, then mergeStreams([...allTranscripts, journal]) (the EXISTING merge.ts already total-orders
  # NormalizedEvent[][] — NO merge change); a SINGLE --transcript stays byte-identical (AC2). BAKE WIRING
  # (AC3): the --cli/--real interpreter + Saga receive the REDUCED view (fits context) yet annotationHash +
  # freeze stay over the FULL scrubbed events (provenance UNCHANGED) — resolved via an OPTIONAL
  # interpret(promptEvents, groundingEvents=promptEvents) param so sourceHash hashes the FULL events while
  # the prompt carries the view (the BeatInterpreter interface stays single-arg; the SDK import stays
  # lazy/R4). The testable builders live under src/bundle/ (vitest include is src/**/*.test.ts — the
  # thin-script precedent); scripts/build-bundle.ts stays thin glue. HARD INVARIANTS: the committed
  # public/bundles/story-10-1.json stays BYTE-IDENTICAL (multi-stream is additive; single --transcript
  # reproduces the prior devMaxEpoch+merge) + committed-bundle*.test.ts unweakened; the SHIPPED bundle
  # stays the 5.5 payload-free projection (replay-bundle.ts UNTOUCHED); the reduced view is BAKE-INPUT ONLY
  # (never shipped/hashed/the freeze input); snippets are scrub-safe (built on scrubResult.scrubbedEvents,
  # path-FREE). This story builds + GATE-PROVES the machinery; the operator runs the real full-session bake
  # afterward (NOT run here — no billed/real LLM, bundle not regenerated with rich content). FULLY
  # gate-verifiable (no pixels): view purity/5-key shape/eventId-preserve/snippet-cap/role+outcome-match
  # (AC1); multi-stream merge + single-transcript byte-identical (AC2); eventId-superset of refs +
  # annotationHash over full events identical to HEAD (AC3); synthetic ~689-event size bound <250KB/<60K
  # tokens (AC4); R1/R4/R5, grep -ril anthropic dist/ = NOTHING, BOTH golden snapshots byte-stable,
  # determinism. NOT 5-3 art / 5-4 deploy (backlog deferred operator/external steps). NO git commit
  # (operator commits between stories). epic-5 stays in-progress (5-6 is not the first Epic-5 story; no
  # epic flip triggered).
  # 5-6 review -> done (senior-review fix round 1, 2026-06-15). Review APPROVED all 8 ACs (shipped bundle +
  # provenance + committed fixture byte-stable); 2 med + 4 low fix/consider + 2 low refute. Two latent code
  # seams FIXED + regression-tested in src/bundle/tagging-view.ts: F1 (stripPaths gutted tag-salient Bash
  # command path args like ./build.sh -> "" — path-stripping now restricted to the tool_result head + the
  # file-op summary via a boundSnippet stripPath flag; the Bash command head, the beat signal §6, stays
  # intact — a transient already-scrubbed bake input) + F2/F3 (snippets now cap on Buffer.byteLength UTF-8
  # not UTF-16 units so the AC4 <250KB byte bound holds for multi-byte content — was ~335KB maxed; the
  # "hard byte budget" comment corrected). F4/F5/F6 doc: File List + Modified entry now list
  # transcript-source-plan.ts(+.test.ts) (the fail-closed CLI-shape planner moved out of the thin script so
  # vitest covers the negative branches); gate count corrected 104/1067 -> 105/1089 (+1 omitted file +3 F1/
  # F2/F3 regression tests); the AC3 "exercised end-to-end" note reworded to "gate-covered via the pure
  # planTranscriptSources". F7 refuted (intentional documented fail-loud typo guard). F8 refuted (Math.max
  # (...[]) -> -Infinity is verbatim PRE-EXISTING at HEAD build-bundle.ts:113-114, unreachable via the
  # script — planTranscriptSources guarantees >=1 source). Gates GREEN: typecheck/lint clean (R1/R4/R5 hold,
  # eslint.config.ts UNTOUCHED), 105 files / 1089 tests, build OK + grep -ril anthropic dist/ = NOTHING,
  # public/bundles/story-10-1.json BYTE-IDENTICAL to HEAD (annotationHash c10c15aa unchanged), BOTH golden
  # snapshots (pace/+ingest/) byte-stable. NO git commit. epic-5 stays in-progress (5-3 art + 5-4 deploy
  # remain; 5-6 is not the Epic-5 capstone).
  5-6: done
  # 5-7: chunked/windowed real-LLM interpret — enables the deferred full-session bake (NOT in epics.md;
  # ACs authored verbatim in the operator's createBrief, the Story 5.5/5.6 build-surfaced precedent).
  # BUILD-SURFACED fix for the ONE open thread: a single `claude -p interpret` over the whole ~689-event
  # reduced tagging-view (Story 5.6) exceeds the 600s execFile timeout (exit 143/SIGTERM) AND can
  # hallucinate refs over a large input. 5.7 adds a PURE orchestrator (src/interpret/chunked-interpret.ts)
  # that windows the reduced view into bounded chunks, interprets each via the SAME 2-arg
  # ClaudeInterpreter.interpret(promptChunk, FULL groundingEvents) so sourceHash/annotationHash stay over
  # the full events (provenance UNCHANGED, the 5.6 contract), then merges + dedups the BeatAnnotation[].
  # Chunked path is --cli/--real ONLY; the mocked dev/CI path (FixtureInterpreter) is UNTOUCHED so
  # public/bundles/story-10-1.json stays BYTE-IDENTICAL. Saga stays one-shot (out of scope). FULLY
  # gate-provable with a FAKE interpreter (ZERO LLM spend, the 3.2/5.6 mocked-machinery precedent): chunk
  # coverage/bound, per-chunk full-grounding, merge+dedup, determinism, mocked-path byte-identical bundle,
  # R1/R4/R5, golden snapshots byte-stable. The real full-session bake is the DEFERRED operator step AFTER
  # this lands (separate explicit OK — billed/real `claude -p`). epic-5 stays in-progress (5-7 is not the
  # Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  # 5-7 moved backlog -> ready-for-dev (create-story). Story file authored at
  # _bmad-output/implementation-artifacts/5-7-chunked-interpret.md (build-surfaced, NOT in epics.md; ACs
  # verbatim from the operator createBrief). PURE orchestrator src/interpret/chunked-interpret.ts:
  # chunkTaggingView windows the Story 5.6 reduced TaggingViewEvent[] into bounded chunks (MAX_CHUNK_EVENTS
  # =100 inline const, ~7 chunks for 689 events, each <35KB/chunk — far under the ~193KB whole-view one-shot
  # that exceeds ClaudeCliClient DEFAULT_TIMEOUT_MS=600_000); interpretChunked calls the SAME 2-arg
  # ClaudeInterpreter.interpret(chunk, FULL groundingEvents) per chunk (the 5.6 seam reused AS-IS, sourceHash
  # /annotationHash stay over the full scrubbed events) via an INJECTED interpret callback (DI -> SDK-free,
  # gate-testable with a FAKE), then mergeAnnotations concatenates in chunk order + dedups (eventRef+beatType,
  # keep highest-confidence, stable first-seen tiebreak). SINGLE wiring change at build-bundle.ts:201
  # (interpreter.interpret -> interpretChunked). Saga stays ONE-SHOT (UNTOUCHED). HARD INVARIANTS: chunked
  # path is --cli/--real ONLY (mocked FixtureInterpreter else-branch UNTOUCHED -> public/bundles/story-10-1
  # .json BYTE-IDENTICAL, annotationHash c10c15aa unchanged, committed-bundle*.test.ts unweakened);
  # annotationHash/freeze over the FULL events (assembleBundle gets scrubResult as-is); BeatInterpreter
  # interface stays single-arg; no Layer-0 code (BOTH golden snapshots byte-stable). FULLY gate-provable with
  # a FAKE interpreter (ZERO LLM spend, the 3.2/5.6 mocked-machinery precedent): AC1 chunk coverage/bound/
  # order/determinism; AC2 per-chunk full-grounding (sourceHash over full events) ; AC3 merge+dedup+freeze-
  # resolves; AC4 ~689-event byte bound + chunk count; AC5 mocked-path byte-identical bundle + R1/R4/R5 +
  # grep -ril anthropic dist/ empty + golden snapshots stable + determinism. Baseline 106 files / 1095 tests
  # at HEAD 16fc207 (adds chunked-interpret.test.ts). The real full-session bake is the DEFERRED operator
  # step AFTER this lands (separate explicit OK — billed/real `claude -p`). NO git commit (operator commits
  # between stories). epic-5 stays in-progress (5-7 is not the Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  # 5-7 moved ready-for-dev -> in-progress -> review (dev-story, baseline 16fc207). Implemented the PURE
  # chunked-interpret orchestrator (src/interpret/chunked-interpret.ts: chunkTaggingView + interpretChunked
  # + mergeAnnotations + MAX_CHUNK_EVENTS=100) and wired the single --cli/--real interpret call in
  # scripts/build-bundle.ts to it over the 5.6 2-arg seam (chunk=prompt, FULL scrubbed events=grounding on
  # every call). Turned the ATDD red suite GREEN (28 tests) + fixed two ATDD-test `role:'self'` typecheck
  # defects honestly (-> 'source', no assertion weakened). Committed bundle BYTE-IDENTICAL (annotationHash
  # c10c15aa…); both golden snapshots byte-stable; grep -ril anthropic dist/ = NOTHING (orchestrator
  # SDK-free + browser-unreachable). Gates: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED),
  # 107 files / 1123 tests (+1 file/+28 = the new suite), build OK. NO git commit (operator commits between
  # stories). The real full-session bake is the DEFERRED operator step (explicit OK after this lands).
  # epic-5 stays in-progress (5-7 is not the Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  # 5-7 moved review -> done (senior-review fix round 1, 2026-06-15). Review APPROVED (all 5 ACs PASS); 0 high
  # / 0 med / 2 low. F1 (fix): corrected stale test counts to verified actuals (suite 28; full 107 files /
  # 1123 tests; delta +28 — the empty-view zero-calls guard + default-maxChunkSize wiring tests are legit
  # coverage); doc-only. F2 (consider): documented in chunked-interpret.test.ts that the AC4 CHUNK_BYTE_BOUND
  # assumes ASCII-payload content (escape-free 100-row chunk = 29001 bytes < 35000; escape-pathological
  # all-quotes = 49001) — hardening, not a bug; the load-bearing RELATIVE whole-view>4x-chunk proof holds
  # under both shapes. NO production code change. Gates re-GREEN: typecheck/lint clean, 107 files / 1123 tests,
  # build OK, grep -ril anthropic dist/ = NOTHING, both golden snapshots byte-stable, bundle byte-identical.
  # NO git commit. epic-5 stays in-progress (5-7 is not the Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  5-7: done
  # 5-8: name-safe Saga — author over the payload-free public surface, not the snippet view (NOT in
  # epics.md; ACs in the operator createBrief, the 5.5/5.6/5.7 build-surfaced precedent). BUILD-SURFACED
  # privacy fix surfaced by the 5.7 real bake: the structured bundle (projectedEvents/annotations) was
  # CLEAN, but the real Opus SAGA prose leaked real identifiers (Vibranto, auth_tokens, SaveProfileSheet,
  # RecoverView, LogMailer, migration 0045, PUBLIC_BASE_URL, FK-23503, …) because SagaAuthor was fed the
  # snippet-bearing reduced tagging-view (5.6 snippets are path-free + secret-scrubbed but NOT
  # symbol/name-free). FIX: author the Saga over ONLY the name-free data the public bundle already ships —
  # projectEvents (5.5 payload-free projection: tool/role/outcome) + the frozen beats + teaching.json — via
  # a PURE buildSagaBrief, plus an explicit anonymization clause in the system prompt. Invariant: the Saga
  # can leak nothing the bundle doesn't already safely expose. Interpret/beats (5.7) UNCHANGED; mocked path
  # uses PLACEHOLDER_SAGA so committed public/bundles/story-10-1.json stays BYTE-IDENTICAL. FULLY
  # gate-provable mocked (zero spend): brief purity/name-free/determinism, mocked SagaAuthor gets the brief,
  # byte-identical bundle, R1/R4/R5, golden snapshots stable. The real Saga RE-BAKE (billed `claude -p`) is
  # the operator step AFTER this lands. epic-5 stays in-progress (5-8 is not the Epic-5 capstone).
  # 5-8 moved backlog -> ready-for-dev (create-story, baseline cdf5081 / registration daa4e44). Story file
  # authored at _bmad-output/implementation-artifacts/5-8-name-safe-saga.md (build-surfaced, NOT in
  # epics.md; ACs VERBATIM from the operator createBrief, the 5.5/5.6/5.7 precedent). The FIX: author the
  # Saga over a PURE buildSagaBrief({projectedEvents (5.5 name-free projection), annotations (frozen beats),
  # teaching.json}) — name-free BY CONSTRUCTION (classifyRole discards the path; projection drops payload/
  # snippet/name) — plus an anonymization clause in SagaAuthor.SYSTEM_PROMPT (belt over the name-free input),
  # rewiring both Saga callers (build-bundle.ts, scribe-saga.ts) off the leaky snippet-bearing tagging-view.
  # FULLY gate-provable mocked (ZERO spend): brief purity/name-free/subset/determinism, mocked SagaAuthor
  # gets the brief, byte-identical committed bundle (mocked path uses PLACEHOLDER_SAGA, never SagaAuthor),
  # R1/R4/R5, golden snapshots stable. Interpret/beats (5.7) UNTOUCHED (tagging-view STAYS the interpret
  # prompt, removed only from the Saga path). The real Saga RE-BAKE (billed `claude -p`) is the operator
  # step AFTER this lands. epic-5 stays in-progress (5-8 is not the Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  # 5-8 moved ready-for-dev -> in-progress -> review (dev-story, baseline cdf5081). Implemented the name-safe
  # Saga: NEW src/scribe/saga-brief.ts (PURE buildSagaBrief over projectedEvents + frozen beats + teaching,
  # name-free BY CONSTRUCTION); SagaAuthor.SYSTEM_PROMPT gained the anonymization clause + DEFAULT_PROMPT_VERSION
  # saga-tolkien-v1 -> saga-tolkien-v2; SagaInput narrowed to SagaBrief (leaky NormalizedEvent[]/TaggingViewEvent[]
  # union dropped); both callers (build-bundle.ts --cli/--real, scribe-saga.ts) rewired off the snippet-bearing
  # tagging-view to the brief. ATDD red tests (saga-brief.test.ts, saga-author.name-safe.test.ts) GREEN + existing
  # saga-author suites updated. Gates GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED),
  # 109 files / 1146 tests (was 107/1123 +2 files/+23 tests), build OK, grep -ril anthropic dist/ = NOTHING,
  # committed bundle BYTE-IDENTICAL (566fbaaf... / annotationHash c10c15aa..., mocked path uses PLACEHOLDER_SAGA),
  # both golden snapshots byte-stable, two bundle runs byte-identical. Interpret/beats (5.7) + replay-bundle
  # schema UNTOUCHED (AC5). NO git commit (operator commits). The real Saga RE-BAKE is the deferred operator step.
  # 5-8 moved review -> done (senior-review fix round 1, 2026-06-15). Review: 0 high / 1 med / 5 low. The med
  # F1 (the one channel NOT name-free by construction — toolName carries mcp__<product>__<action>/custom names
  # verbatim, unmatched by any scrub pattern) is CLOSED STRUCTURALLY: buildSagaBrief now neutralizes any
  # non-built-in tool name to the neutral 'tool' token (BUILT_IN_TOOL_NAMES = the translation-rules.json verbs +
  # standard Claude Code built-ins), proven by a new mcp__* fixture test. F3 (scribe-saga.ts DEFAULT_PROMPT_VERSION
  # bumped v1->v2 in lockstep with SagaAuthor) + F5 (name-free path-marker proof scoped to events/beats so editing
  # teaching.json can't break it) fixed. F2 (battleTimeline forward-compat seam, read by nothing), F4 (teaching
  # pass-by-ref, no mutation), F6 (beats-less/un-scrubbed standalone brief, name-safe via projectEvents; residual
  # toolName closed by F1) REFUTED. Gates re-GREEN: typecheck/lint clean (R1/R4/R5 hold, eslint.config.ts UNTOUCHED),
  # 109 files / 1147 tests (was 1146; +1 F1 test), build OK, grep -ril anthropic dist/ = NOTHING, committed bundle
  # BYTE-IDENTICAL (annotationHash c10c15aa...), both golden snapshots byte-stable, AC5 files untouched. NO git
  # commit (operator commits). epic-5 stays in-progress (5-8 is not the Epic-5 capstone; 5-3 art + 5-4 deploy remain).
  5-8: done
  # 5-9: publish-enabler — decouple the boot's static default bundle from the SHIPPED artifact (direct
  # operator change, NOT a pipeline story; verified by full gates). The Story 5.7/5.8 real bake is clean
  # (667 payload-free events, 47 beats, name-safe Saga) but publishing it as public/bundles/story-10-1.json
  # broke ~8 startArena boot suites: arena-boot.ts statically imported the SHIPPED bundle as startArena's
  # default, and those suites assert the FIXTURE's two beats (dispel@u-0002#1 + shaman@u-0010#0) which the
  # real 47 UUID-keyed beats don't match. FIX: arena-boot.ts now imports a DEDICATED committed fixture
  # bundle (src/render/__fixtures__/fixture-bundle.json = the prior fixture content) as startArena's default;
  # production (main.ts -> loadBundle fetch) is unaffected (the static import tree-shakes from prod). Rescoped
  # the session-ingest annotationHash guard to the fixture bundle (c10c15aa) and made committed-bundle.test.ts
  # content-agnostic + added a STRUCTURAL Saga name-safety regression guard (no path/ext/snake_case — no real
  # identifier hard-coded). Gates GREEN on the decouple (public/ still fixture): 109 files / 1149 tests, R4
  # clean. The PUBLISH (swap public/bundles/story-10-1.json -> the real frozen bundle) is the next commit.
  # epic-5 stays in-progress.
  5-9: done
