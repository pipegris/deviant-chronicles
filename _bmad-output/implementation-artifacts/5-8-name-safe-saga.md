---
baseline_commit: cdf5081
registration_commit: daa4e44
---

# Story 5.8: Name-safe Saga — author over the payload-free public surface, not the snippet view

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As the operator publishing the public showcase,
I want the closing Saga authored over ONLY the name-free data the public bundle ALREADY ships (the payload-free `projectedEvents` projection + the frozen beats + the teaching concepts) plus an explicit anonymization instruction,
so that the lush fantasy Saga can leak NOTHING the bundle doesn't already safely expose — while the interpret/beats (5.7) and the committed fixture bundle stay UNCHANGED. (FR-10, NFR-5; extends the Story 5.5 payload-free hard line to Layer 2/Told.)

## Acceptance Criteria

_Author's note: this story is a BUILD-SURFACED privacy fix, surfaced by the Story 5.7 REAL full-session bake; it is NOT in `epics.md` (there is no `epics.md` anchor — the ACs live in the operator's createBrief, the Story 5.5/5.6/5.7 build-surfaced precedent). The Given/When/Then ACs below are the authoritative source for this story, authored VERBATIM per the operator. They extend the Epic-5 bake machinery (`epics.md#Story-5.2` build orchestration; `epics.md#Story-4.2` the offline `SagaAuthor`) and, crucially, the Story 5.5 payload-free HARD LINE (the public bundle must NEVER ship a real file/symbol name or path) — pushing that line into Layer 2/Told. They touch the `--cli`/`--real` Saga authoring INPUT + SYSTEM PROMPT ONLY; nothing about the shipped bundle schema, the committed fixture, or `annotationHash`/freeze provenance changes._

_WHAT HAPPENED (verified, this session): the Story 5.7 chunked interpret worked — the real bake produced 667 payload-free events + 47 real beats, and EVERY structured surface (`projectedEvents`, `annotations`, `battleTimeline`, scrub, `assetManifest`, `tuningConfig`) was CLEAN (name-free, path-free, secret-free). BUT the real Opus SAGA prose leaked real identifiers from the source session: the product name `Vibranto`, DB tables (`auth_tokens`, `users`), `migration 0045`, component/file names (`SaveProfileSheet`, `RecoverView`, `LogMailer`, `identity-client`), an env var (`PUBLIC_BASE_URL`), `FK-23503`, library names (Drizzle/Hono/zod), `expires_at`, `COALESCE`, `magic-link`, etc. ROOT CAUSE: `SagaAuthor.authorSaga(events)` is fed the snippet-bearing reduced tagging-view (`src/bundle/tagging-view.ts`) whose per-event `snippet` is path-free + secret-scrubbed but NOT symbol/name-free (it carries Bash command heads, tool_result heads, assistant-text heads — which contain real table/component/symbol names). The Saga `JSON.stringify`'s that input into its prompt, so the prose absorbed the names. This crosses the operator HARD LINE (Story 5.5)._

**AC1 — name-free Saga input.**
**Given** the `--cli`/`--real` bake,
**When** the Saga is authored,
**Then** its prompt input is assembled by a PURE `buildSagaBrief(...)` from ONLY name-free material the public bundle ALREADY ships — the payload-free `projectedEvents` (tool/role/outcome; the Story 5.5 `projectEvents` output) + the frozen beats (`beatType`/`confidence`/order) + the teaching concepts (`src/config/teaching.json`) (optionally a name-free `battleTimeline` arc summary) — and carries NO snippet, NO payload, NO raw path, NO file/symbol/table/column/component/product/env name. Deterministic (same input → same brief), no mutation.

**AC2 — anonymization instruction (belt over the name-free input).**
**Given** the Saga system prompt,
**Then** it EXPLICITLY forbids emitting OR inventing any real product/project/file/module/class/function/table/column/env-var/library name and instructs fantasy/role/concept language ONLY (so the model cannot fabricate a plausible real-looking identifier even though the input is already name-free). The fail-loud empty-Saga guard is preserved.

**AC3 — the Saga cannot exceed the bundle's public surface.**
**Given** the assembled brief,
**Then** it is a subset/derivative of what `ReplayBundle` ALREADY ships publicly (`projectedEvents` + `annotations`) + `teaching.json` — provably name-free: a unit asserts the serialized brief contains NO `snippet` field, NO path markers (e.g. `/`, `.ts`, `src/`), and none of a representative real-identifier set; and `buildSagaBrief` is pure/deterministic.

**AC4 — committed bundle byte-identical + mocked path untouched.**
**Given** the default dev/CI mocked path,
**When** `pnpm bundle:story-10-1` runs,
**Then** it STILL uses `PLACEHOLDER_SAGA` (NOT `SagaAuthor`) so the committed `public/bundles/story-10-1.json` stays BYTE-IDENTICAL; the `SagaAuthor` input-type change is covered by updating `saga-author.test.ts` + the two callers (`scripts/build-bundle.ts`, `scripts/scribe-saga.ts`); R1/R4/R5 hold (`eslint.config.ts` UNTOUCHED), `grep -ril anthropic dist/` = NOTHING, BOTH golden snapshots byte-stable, determinism.

**AC5 — interpret/beats (5.7) UNCHANGED.**
**Given** this story,
**Then** it changes ONLY the Saga authoring input + system prompt; `src/interpret/chunked-interpret.ts`, `claude-interpreter.ts`, the annotations/freeze/`annotationHash`, and the shipped bundle schema (`replay-bundle.ts`) are UNTOUCHED.

### Scope fence (READ FIRST — what this story IS and IS NOT)

- **IS:** make the Saga name-safe by authoring it over the already-public name-free surface. It adds (1) a PURE `src/scribe/saga-brief.ts` `buildSagaBrief(projectedEvents + annotations + teaching [+ arc])` that returns a compact, name-free, JSON-serializable `SagaBrief`; (2) an anonymization clause appended to `SagaAuthor`'s `SYSTEM_PROMPT`; (3) rewiring the two Saga callers (`scripts/build-bundle.ts`, `scripts/scribe-saga.ts`) so the `--cli`/`--real` Saga receives the BRIEF (not the tagging-view / not the full events); (4) the `SagaAuthor` input-type change + tests. It builds the MACHINERY; the operator RE-BAKES the Saga afterward (NOT run here).
- **IS NOT:** an interpret/beats change (Story 5.7 UNTOUCHED — `chunked-interpret.ts`/`claude-interpreter.ts`/`fixture-interpreter.ts`/`freeze.ts` are not touched); a shipped-bundle schema change (`replay-bundle.ts` UNTOUCHED); a real bake run (this story makes NO LLM call and does NOT regenerate the bundle with rich content — the operator re-bakes the Saga AFTER this lands, billed, with explicit OK); a committed-bundle change (byte-identical — the mocked path uses `PLACEHOLDER_SAGA`, never `SagaAuthor`); a Layer-0 change (ingest/translate/pace/model UNTOUCHED; BOTH golden snapshots byte-stable); a new beat tag or `RenderPort` method; a change to the Story 5.5 `projectEvents` projection (REUSED AS-IS) or to the Story 5.6 reduced tagging-view (`tagging-view.ts` UNTOUCHED — it is NO LONGER the Saga input, but it is still the interpret prompt input, so it STAYS). It does NOT touch Stories 5-3 (art) / 5-4 (deploy) — those remain backlog deferred operator/external steps.
- **HARD INVARIANTS (load-bearing, do NOT cross):**
  1. **The committed fixture bundle stays BYTE-IDENTICAL.** `public/bundles/story-10-1.json` is regenerated by `pnpm bundle:story-10-1` (the mocked path, which uses `PLACEHOLDER_SAGA`, NEVER `SagaAuthor`) and must come out byte-for-byte identical to HEAD (`annotationHash` `c10c15aa…` unchanged). `committed-bundle.test.ts` + `committed-bundle.payload-free.test.ts` stay GREEN and are NOT weakened.
  2. **The Saga input is a SUBSET of the PUBLIC bundle surface** (`projectedEvents` + `annotations`) + `teaching.json` — provably name-free: NO `snippet`/payload/path/name reaches the Saga prompt (AC1/AC3). This is the Story 5.5 hard line, now enforced for Layer 2/Told.
  3. **interpret + annotations + `annotationHash`/freeze stay UNCHANGED** (Story 5.7 / 5.6 contract). The interpret prompt is STILL the reduced tagging-view (chunked); the Saga input is now the name-free brief — they DIVERGE. `assembleBundle` is still called with `scrubResult` UNCHANGED, so `annotationHash`/freeze stay over the FULL scrubbed events.
  4. **R1/R4/R5 hold; `saga-brief.ts` is SDK-free + phaser-free; golden snapshots byte-stable.** `eslint.config.ts` is UNTOUCHED.

### Gate-verifiable vs operator-verified (this story is FULLY gate-verifiable, mocked — ZERO spend)

- **GATE-VERIFIABLE (Vitest, with a FAKE client — ZERO spend, the Story 4.2/5.6/5.7 mocked-machinery precedent — this is what "done" means):**
  - **(AC1/AC3)** `buildSagaBrief(projectedEvents, annotations, teaching[, arc])` is PURE/deterministic (call-twice `toEqual`; no input mutation — deep-snapshot inputs, assert deep-equal after); the brief is built from ONLY the name-free inputs; a name-free unit asserts the SERIALIZED brief (`JSON.stringify`) contains NO `snippet` substring, NO path markers (`/`, `\`, `.ts`, `.json`, `src/`), and NONE of a representative real-identifier set (`Vibranto`, `auth_tokens`, `users`, `SaveProfileSheet`, `RecoverView`, `LogMailer`, `identity-client`, `PUBLIC_BASE_URL`, `FK-23503`, `migration 0045`, `Drizzle`, `Hono`, `zod`, `expires_at`, `COALESCE`, `magic-link`); a subset proof asserts every key the brief carries derives ONLY from `projectedEvents`/`annotations`/`teaching` (no field traceable to `payload`/`snippet`/`path`).
  - **(AC2)** a unit asserts `SagaAuthor`'s default `SYSTEM_PROMPT` carries the anonymization clause (substring assert — it forbids real product/project/file/module/class/function/table/column/env-var/library names and instructs fantasy/role/concept language ONLY); the empty/whitespace-only fail-loud guard is preserved (the existing `saga-author.unit.test.ts` empty-response throws stay GREEN).
  - **(AC1/AC4 — the mocked SagaAuthor receives the BRIEF)** an injected FAKE `AnthropicLike` client records the request `body`; a unit drives `SagaAuthor.authorSaga(buildSagaBrief(...))` and asserts the user-message content (the serialized prompt) is the BRIEF (re-parse it; assert it has the brief's shape, NO `snippet` field, none of the real-identifier set), NOT the snippet-bearing tagging-view nor the full events.
  - **(AC4)** `pnpm bundle:story-10-1` (mocked path) yields a `public/bundles/story-10-1.json` byte-identical to HEAD (`git diff` empty; `annotationHash` `c10c15aa…` unchanged); R1/R4/R5 discipline (`saga-brief.ts` is SDK-free + phaser-free; `eslint.config.ts` UNTOUCHED); `grep -ril anthropic dist/` empty; BOTH golden snapshots byte-stable; determinism (two `pnpm bundle:story-10-1` runs byte-identical).
  - **(AC5)** a `git diff HEAD` shows `src/interpret/chunked-interpret.ts`, `src/interpret/claude-interpreter.ts`, `src/interpret/freeze.ts`, `src/interpret/fixture-interpreter.ts`, `src/bundle/assemble-bundle.ts`, `src/bundle/tagging-view.ts`, and `src/schema/replay-bundle.ts` are UNTOUCHED.
  - Plus: `pnpm typecheck` clean (strict); `pnpm lint` clean; `pnpm test` green (baseline 107 files / 1123 tests at HEAD `cdf5081`; this story ADDS the `saga-brief` suite + a `saga-author` anonymization-clause/brief-input case); `pnpm build` succeeds.
- **OPERATOR-VERIFIED (NOT a gate, DEFERRED — the operator runs it later, with explicit OK):** the real Saga RE-BAKE (billed `claude -p`) over the name-free brief reads well AND re-greps name-free. This story does NOT run it; it builds + gate-proves the name-safe Saga machinery so the operator's run is a no-code-change step.

## Tasks / Subtasks

- [x] **Task 1 — The PURE name-free Saga brief builder (AC1, AC3).** (NEW `src/scribe/saga-brief.ts` + co-located `src/scribe/saga-brief.test.ts`)
  - [x] Author the brief shape as a plain TypeScript `interface` (NOT Zod — it is a TRANSIENT in-memory bake-prompt input, never serialized to a shippable artifact, never read from an untrusted source — the same call `TaggingViewEvent` / `TeachingOp` make; §1). It carries ONLY name-free, derived-from-public-surface data:
    ```ts
    export interface SagaBriefEvent {
      // EXACTLY the ProjectedEvent public fields the Saga needs for the arc — all name-free.
      // toolName is the abstract tool ('Bash'/'Edit'/'Read'/null), NOT a path/symbol; role is the
      // coarse classifyRole token (test/schema/migration/config/doc/source); outcome is success/isError.
      eventType: string;
      toolName: string | null;
      role: AbstractedRole; // reuse the Story 5.5 union (schema/replay-bundle)
      outcome: Outcome;     // reuse the Story 5.5 union (schema/replay-bundle)
    }
    export interface SagaBriefBeat {
      beatType: BeatType;   // shaman | dispel | summon (schema/beat-annotation)
      confidence: number;   // the 0–1 self-rating (order-preserving, the arc spine)
    }
    export interface SagaBrief {
      events: SagaBriefEvent[];          // from projectedEvents (Story 5.5), name-free
      beats: SagaBriefBeat[];            // from annotations (frozen), name-free
      teaching: TeachingTable;           // the shaman/dispel/summon concepts (teaching.json) — safe narrative material
      arc?: SagaBriefArc;                // OPTIONAL name-free battleTimeline summary (§3) — decide on merits + document
    }
    ```
  - [x] `export function buildSagaBrief(args: { projectedEvents: ProjectedEvent[]; annotations: BeatAnnotation[]; teaching: TeachingTable; battleTimeline?: BattleTimeline }): SagaBrief` — PURE/deterministic (R2 posture even though `src/scribe/` is not a Layer-0 dir — keep it `Date.now`/`Math.random`/IO/global-mutable-free for determinism + testability). SDK-free + phaser-free. Map each `ProjectedEvent` to a `SagaBriefEvent` keeping ONLY `{eventType, toolName, role, outcome}` (DROP `orderKey`/`eventId` — the Saga makes no truth claim and grounds NOTHING; carrying ids buys nothing and keeps the brief minimal — decide + document); map each `BeatAnnotation` to a `SagaBriefBeat` keeping ONLY `{beatType, confidence}` (DROP `eventRef`/`groundingPointer`/`sourceHash`/`interpreterVersion` — those carry no narrative arc and the eventRefs are opaque ids the Saga has no use for); pass `teaching` through verbatim (it is already the validated `TeachingTable` — three safe one-liners). Fresh objects; input untouched.
  - [x] **DECISION — the OPTIONAL `arc` summary (§3, decide on merits + document).** The createBrief offers "(optionally a name-free `battleTimeline` arc summary)". RESOLVE: include a MINIMAL, provably name-free arc summary `SagaBriefArc { beatCount: number; finalState: '…' }` derived ONLY from name-free `battleTimeline` aggregates (e.g. the number of beats and a coarse final-victory marker), OR OMIT it entirely if the events+beats+teaching already carry the closing arc. Default to the SIMPLER choice (events + beats + teaching) UNLESS the arc materially improves the Saga's shape — and if you include `arc`, derive it ONLY from name-free numeric/enum aggregates (NEVER `sourceEventIds`, NEVER any path/name). Document the decision and WHY in Dev Notes. The `battleTimeline` arg is OPTIONAL so the builder works with or without it.
  - [x] `src/scribe/saga-brief.test.ts` (AC1/AC3): build a brief from a representative `ProjectedEvent[]` + `BeatAnnotation[]` + the real `TEACHING` table. Assert: (a) the brief carries EXACTLY the documented keys per row (`Object.keys` set equality on `events[i]`, `beats[i]`); (b) `events.length === projectedEvents.length`, `beats.length === annotations.length`, order preserved; (c) determinism — call twice, `toEqual`; (d) no input mutation — deep-snapshot the inputs, assert deep-equal after. **The NAME-FREE proof (the load-bearing test):** construct `ProjectedEvent[]` + `BeatAnnotation[]` whose source fields WOULD carry leaks IF the builder ever pulled from payload (it cannot — `ProjectedEvent`/`BeatAnnotation` have no payload/snippet field), then serialize `JSON.stringify(buildSagaBrief(...))` and assert it `.not.toContain` each of: `'snippet'`, `'/'`, `'.ts'`, `'.json'`, `'src/'`, AND each of the representative real-identifier set (`Vibranto`, `auth_tokens`, `users`, `SaveProfileSheet`, `RecoverView`, `LogMailer`, `identity-client`, `PUBLIC_BASE_URL`, `FK-23503`, `migration 0045`, `Drizzle`, `Hono`, `zod`, `expires_at`, `COALESCE`, `magic-link`). (Belt note: the only string content that COULD appear is the teaching one-liners + the enum tokens — assert the teaching one-liners ARE present, proving the safe narrative material flows through.) **The SUBSET proof (AC3):** assert the brief's `events` are a field-restriction of `ProjectedEvent` (already-public) and `beats` a field-restriction of `BeatAnnotation` (already-public), so the brief ⊆ `{projectedEvents, annotations}` + `teaching.json` by construction.
  - [x] **R-boundary note (verify, document):** `src/scribe/saga-brief.ts` imports `ProjectedEvent`/`Outcome`/`AbstractedRole` types from `../schema/replay-bundle`, `BeatAnnotation`/`BeatType` from `../schema/beat-annotation`, `BattleTimeline` from `../schema/battle-timeline` (TYPES), and the validated `TEACHING`/`TeachingTable` from `../portal/teaching-config` (a VALUE import). It imports `projectEvents` from `../bundle/project-events` ONLY IF the caller does not already have `projectedEvents` (it WILL — `assembleBundle` already projects; the caller passes `projectedEvents` in). **The `src/scribe/ → src/bundle/` and `src/scribe/ → src/portal/` edges are ALLOWED** (eslint only restricts Layer-0→interpret (R1), anthropic (R4), phaser (R5); `saga-author.ts:2` already imports `TaggingViewEvent` from `../bundle/`). `teaching-config.ts` is SDK-free + phaser-free + browser-reachable already, so importing its `TEACHING` value into a bake-time builder is clean. `saga-brief.ts` is SDK-free + phaser-free — verify by a source-grep assertion in its test (it is a bake-input builder, browser-UNREACHABLE like `saga-author.ts`, so do NOT add it to `r1-discipline.test.ts`'s `SCRIBE_MODULES` browser-reader list — that list is for shipped readers; assert SDK/phaser-freedom in `saga-brief.test.ts` instead).

- [x] **Task 2 — The anonymization clause in `SagaAuthor`'s SYSTEM_PROMPT (AC2).** (`src/scribe/saga-author.ts`)
  - [x] Append an EXPLICIT anonymization instruction to the existing `SYSTEM_PROMPT` constant (`saga-author.ts:65-72`). It must: (a) FORBID emitting OR inventing any real product/project/file/module/class/function/table/column/env-var/library name; (b) instruct fantasy/role/concept language ONLY (the Forgemaiden / curses / binding-spell register the prompt already uses); (c) make clear the model receives ONLY abstract role/concept data and must NOT fabricate a plausible real-looking identifier to fill the gap. Keep it a documented module constant (the established pattern — NOT externalized to config); it stays overridable via the `systemPrompt?` constructor option (the existing config-knob seam). Do NOT bump `DEFAULT_PROMPT_VERSION` UNLESS the operator re-bake needs a distinct stamp — DECIDE + document: a system-prompt content change SHOULD bump the prompt version so the re-baked Saga's provenance reflects the anonymization-hardened prompt; bump `DEFAULT_PROMPT_VERSION` to `'saga-tolkien-v2'` and update the one unit asserting the default (`saga-author.unit.test.ts:42`) — this is a metadata stamp, not a behavior change, and the committed bundle is unaffected (mocked path uses `FIXTURE_PROMPT_VERSION`/`PLACEHOLDER_SAGA`, never `SagaAuthor`). Confirm the version bump does NOT alter the committed `public/bundles/story-10-1.json` (it does not — the mocked path's `promptVersion` is `FIXTURE_PROMPT_VERSION = 'fixture-v1'`, set in `build-bundle.ts`, independent of `SagaAuthor.DEFAULT_PROMPT_VERSION`).
  - [x] PRESERVE the fail-loud empty-Saga guard verbatim (`saga-author.ts:108-119` — a refusal/empty must still throw). AC2 explicitly requires it intact.
  - [x] Update the SagaAuthor doc comment (the `SYSTEM_PROMPT` block, the `authorSaga` block) to say the input is now the NAME-FREE `SagaBrief` (the public-surface projection + beats + teaching), NOT the full events nor the snippet-bearing tagging-view, and WHY (the Story 5.5 hard line for Layer 2).

- [x] **Task 3 — Widen the `SagaAuthor` input type to accept the brief (AC4).** (`src/scribe/saga-author.ts` + `src/scribe/saga-author.test.ts`)
  - [x] `authorSaga(events: SagaInput)` currently accepts `SagaInput = readonly NormalizedEvent[] | readonly TaggingViewEvent[]` (the Story 5.6 widening). The Saga only `JSON.stringify`'s its input (no truth claim, no provenance), so the input is OPAQUE — RESOLVE the type minimally (decide + document, §4): REPLACE `SagaInput` with `SagaBrief` (the brief is now the ONLY production Saga input) — OR widen it to `SagaInput = SagaBrief | readonly NormalizedEvent[] | readonly TaggingViewEvent[]` if any caller/test still passes the legacy shapes. **PREFERRED:** make `SagaBrief` the canonical input and DROP the `NormalizedEvent[] | TaggingViewEvent[]` union members IF no remaining production caller needs them (both callers are rewired in Task 4) — this is the cleaner, intent-revealing type and removes the now-dead `TaggingViewEvent` import (which is the LEAK-shaped type we are removing from the Saga path). Keep the existing `saga-author.test.ts` tests GREEN: they pass `runIngest()` (a `NormalizedEvent[]`) — if you narrow `SagaInput` to `SagaBrief`, UPDATE those tests to pass a `buildSagaBrief(...)` over the fixture-derived public surface instead (the AC4 task explicitly authorizes "updating `saga-author.test.ts`"). Use `import type` for `SagaBrief` (erased at runtime → no scribe→scribe runtime concern; `saga-brief.ts` is SDK-free so even a value import would be clean, but the type is all that's needed).
  - [x] **`saga-author.test.ts` updates (AC1/AC2/AC4):** (a) rebuild the test input as `buildSagaBrief({ projectedEvents, annotations, teaching: TEACHING })` over the committed fixture's public surface (project the fixture events via `projectEvents`, use `fixtureAnnotations()` for the beats) so the "carries the event window as the user message" test now asserts the user content re-parses to the BRIEF shape (NOT eventIds — the brief has no eventIds; assert it has `events`/`beats`/`teaching` and `events.length === projectedEvents.length`); (b) ADD an anonymization-clause case: assert the default `SagaAuthor().` (or the `SYSTEM_PROMPT` constant) carries the anonymization substring; (c) ADD a name-free input case: assert the serialized user-message content `.not.toContain` `'snippet'`, the path markers, and the real-identifier set (re-proving the brief is what reaches the prompt). Keep the model/no-tools/max_tokens/fail-loud cases GREEN (rephrase only what the input-shape change forces).

- [x] **Task 4 — Rewire the two Saga callers to pass the name-free brief (AC1, AC4, AC5).** (`scripts/build-bundle.ts`, `scripts/scribe-saga.ts` — thin glue only)
  - [x] `scripts/build-bundle.ts` (the `--cli`/`--real` branch, line ~219): the Saga is currently `saga = await author.authorSaga(taggingView)` — REPLACE the input with the name-free brief. The branch ALREADY has the public surface in hand: `annotations` (the chunked-interpret result) and `scrubResult` (the scrubbed events). Build `const projectedEvents = projectEvents(scrubResult.scrubbedEvents)` (REUSE the Story 5.5 projector — `src/bundle/project-events.ts`, the SAME projection `assembleBundle` ships) and `const sagaBrief = buildSagaBrief({ projectedEvents, annotations, teaching: TEACHING, battleTimeline })`, then `saga = await author.authorSaga(sagaBrief)`. Import `buildSagaBrief` from `../src/scribe/saga-brief`, `projectEvents` from `../src/bundle/project-events`, and `TEACHING` from `../src/portal/teaching-config` (all SDK-free static imports — they do NOT pull `@anthropic-ai/sdk`; only the existing LAZY `await import('../src/scribe/saga-author')` does, unchanged). CRITICAL (Hard Invariants 1 + 3): the interpret path is UNCHANGED — `interpretChunked` STILL receives `taggingView` as its `promptView` and `scrubResult.scrubbedEvents` as grounding (the tagging-view STAYS the interpret prompt, it is ONLY removed from the SAGA path); `assembleBundle` is still called with `scrubResult` UNCHANGED so `annotationHash`/freeze stay over the full events; the `else` (mocked) branch is UNTOUCHED (it uses `fixtureAnnotations()` + `PLACEHOLDER_SAGA`, NEVER `SagaAuthor`, NEVER builds a brief — so the committed bundle stays byte-identical). Update the script header usage comment to document that on `--cli`/`--real` the Saga is now authored over the NAME-FREE public-surface brief (projectedEvents + beats + teaching), distinct from the interpret prompt (the reduced tagging-view).
  - [x] `scripts/scribe-saga.ts` (the standalone Saga baker): it currently feeds the full payload-bearing `events` to `author.authorSaga(events)` (lines 100-115). REWIRE it the SAME way: after ingest + (it has no scrub step today — it ingests raw, then authors) — DECIDE + document: `scribe-saga.ts` is a standalone Saga baker that does NOT currently scrub. To honor the hard line it must author over the name-free brief too. RESOLVE: scrub the ingested events (`scrubSession` with `COMPILED_SCRUB_PATTERNS`) THEN `projectEvents` THEN `buildSagaBrief` — OR, simpler and consistent with `build-bundle.ts`: build the brief from `projectEvents(scrubbedEvents)` + the beats it has access to. **PREFERRED (minimal, correct):** `scribe-saga.ts` has NO interpret step (it bakes the Saga standalone), so it has no `annotations`; pass `beats: []` (an empty beat list) OR compute beats only if the script already does — it does NOT. So build `buildSagaBrief({ projectedEvents: projectEvents(scrubSession(events, COMPILED_SCRUB_PATTERNS).scrubbedEvents), annotations: [], teaching: TEACHING })`. Document that `scribe-saga.ts` produces a beats-less brief (it is the standalone Saga baker; the build-bundle path is the one with real beats). If this materially weakens the standalone Saga, NOTE it — the operator's canonical bake is `build-bundle.ts --cli` (which HAS beats); `scribe-saga.ts` is a convenience baker. KEEP the default (no-flag) `PLACEHOLDER_SAGA` path UNTOUCHED (it never builds a brief). Confirm the static `projectEvents`/`buildSagaBrief`/`scrubSession`/`TEACHING` imports stay SDK-free (only the existing lazy `import('../src/scribe/saga-author')` pulls the SDK).
  - [x] **DECISION (record):** if rewiring `scribe-saga.ts` to scrub+project is more than thin glue or risks scope-creep (it adds a scrub step the script lacked), the MINIMAL alternative is to make `scribe-saga.ts` build the brief from the UN-scrubbed projection — but that is WRONG (the projection's `role`/`outcome`/`toolName` are name-free regardless of scrub, BUT the source events should still be scrubbed for consistency with the hard line; `projectEvents` already drops payload/path/name, so the projection IS name-free even over un-scrubbed events — `classifyRole` discards the path, `outcome` is a bool, `toolName` is the abstract tool). So the SIMPLEST correct choice is: `buildSagaBrief({ projectedEvents: projectEvents(events), annotations: [], teaching: TEACHING })` — `projectEvents` makes it name-free WITHOUT needing a scrub step in this script, because the projection structurally drops every leak vector. PREFER this (no new scrub dependency in `scribe-saga.ts`); document that the projection is name-free by construction (Story 5.5), so the standalone baker needs no scrub to be name-safe at the Saga boundary.

- [x] **Task 5 — Regenerate the committed bundle (must be byte-identical) + gates (all ACs).** (`public/bundles/story-10-1.json`; gates)
  - [x] Re-run `pnpm bundle:story-10-1` (the mocked path — NO `--cli`/`--real`, so `SagaAuthor`/`buildSagaBrief` never run; `PLACEHOLDER_SAGA` is used) and confirm `public/bundles/story-10-1.json` is BYTE-IDENTICAL to HEAD (Hard Invariant 1 — `git diff` empty; `annotationHash` stays `c10c15aa…`). If it differs, the rewiring perturbed the mocked path — FIX it (the `else` branch must be untouched). Stage nothing new for the bundle (it is unchanged).
  - [x] `pnpm typecheck` clean (strict). `pnpm lint` clean — R1/R4/R5 hold, `eslint.config.ts` UNTOUCHED. NEVER relax a boundary to pass. `src/scribe/saga-brief.ts` is SDK-free + phaser-free + imports only `schema/` types + `../portal/teaching-config` (value) + `../bundle/project-events` (only if it projects internally — prefer the caller passing `projectedEvents`) — all allowed for `src/scribe/`.
  - [x] `pnpm test` green — full suite (baseline 107 files / 1123 tests at HEAD `cdf5081`; this story ADDS the `saga-brief.test.ts` suite + updates `saga-author.test.ts`/`saga-author.unit.test.ts`). BOTH golden snapshots (`src/pace/__snapshots__/` + the ingest snapshot) byte-stable — this story adds NO Layer-0 code.
  - [x] `pnpm build` succeeds → `dist/`. `grep -ril anthropic dist/` returns NOTHING (R4 — `saga-brief.ts` is SDK-free; `saga-author.ts`'s SDK import stays lazy/scripts-only; the brief wiring lives in `scripts/` + `src/scribe/saga-brief.ts`, browser-unreachable — `saga-brief.ts` is imported only by `scripts/*` + its own test, never on the `main.ts → render` path). The committed `dist/bundles/story-10-1.json` is the unchanged payload-free bundle.
  - [x] Determinism: two `pnpm bundle:story-10-1` runs yield byte-identical `public/bundles/story-10-1.json`.
  - [x] **AC5 verification:** `git diff HEAD --stat` shows `src/interpret/chunked-interpret.ts`, `claude-interpreter.ts`, `freeze.ts`, `fixture-interpreter.ts`, `src/bundle/assemble-bundle.ts`, `src/bundle/tagging-view.ts`, `src/schema/replay-bundle.ts` UNTOUCHED.
  - [x] Do NOT `git commit` (the operator commits between stories). This story regenerates NOTHING that ships (the committed bundle is byte-identical); it builds + gate-proves the name-safe Saga machinery. The single real Saga RE-BAKE (billed `claude -p`) is a DEFERRED operator step run AFTER this lands, with explicit OK.

### Review Follow-ups (AI)

Fix round 1 — addressing the Senior Developer Review (AI) findings. All gates re-run GREEN after the fixes (typecheck/lint clean; full suite 1147 tests; build OK + dist anthropic-free; committed bundle byte-identical `annotationHash c10c15aa…`; AC5 files untouched).

- [x] **[F1] (med, fixed) — `toolName` now name-free by construction for MCP/custom tools.** `src/scribe/saga-brief.ts`: added a closed `BUILT_IN_TOOL_NAMES` set (Bash/Edit/Write/MultiEdit/NotebookEdit/Read/Grep/Glob/Task/WebFetch/WebSearch/TodoWrite — the translation-rules.json verbs + the remaining standard Claude Code built-ins) and `abstractToolName()`: any non-built-in name (e.g. `mcp__clickup__create_task`) collapses to the neutral `'tool'` token before it reaches `SagaBriefEvent.toolName`. This restores the name-free-BY-CONSTRUCTION guarantee for the one channel scrub did not cover (no scrub pattern matches `mcp__<product>__<action>`). Proven by a new fixture test (`saga-brief.test.ts`) asserting an `mcp__*` toolName becomes `'tool'`, a built-in (`Bash`) passes through, and the serialized brief contains neither `mcp__` nor the product token.
- [x] **[F2] (low, refuted) — `battleTimeline` arg accepted but unused.** No change. VERIFIED intentional forward-compat seam (Dev Notes §3) and SAFE: `battleTimeline` carries `sourceEventIds` (opaque ids) but the builder reads NOTHING from it, so no id can leak — the omission is itself the name-safety property.
- [x] **[F3] (low, fixed) — `scribe-saga.ts` `DEFAULT_PROMPT_VERSION` bumped `saga-tolkien-v1` → `saga-tolkien-v2`** to stay in lockstep with `SagaAuthor.DEFAULT_PROMPT_VERSION`, so the placeholder bake's provenance stamp is not misleadingly stale. The `--cli`/`--real` paths already stamp `author.promptVersion` (= v2) directly.
- [x] **[F4] (low, refuted) — `teaching` passed by reference, not cloned.** No change. VERIFIED: the builder does not mutate; the no-mutation test passes; the brief is transient and only `JSON.stringify`'d, so the shared alias is harmless in the actual data flow.
- [x] **[F5] (low, fixed) — path-marker leak proof scoped to events/beats.** `src/scribe/saga-brief.test.ts`: the "NO path markers" assertion now serializes only `{events, beats}` (the surface that derives from session data, where a path/snippet leak would actually surface), NOT the operator-editable `teaching.json` one-liners — so a future safe one-liner containing a legitimate `'/'` (a date, "and/or") cannot break the leak proof. The teaching strings' own safety is still asserted (they flow through verbatim).
- [x] **[F6] (low, refuted) — standalone `scribe-saga.ts` brief is beats-less + over an un-scrubbed projection.** No change. VERIFIED name-safe: `projectEvents` structurally drops payload/path/name even over un-scrubbed events (documented §3 PREFERRED minimal choice; `build-bundle.ts` is the canonical bake WITH beats). The only residual on this path was the `toolName` channel, now closed under F1.

## Dev Notes

This is a build-surfaced PRIVACY FIX, surfaced by the Story 5.7 real full-session bake, decided with the operator. The structured bundle was CLEAN; ONLY the Opus SAGA prose leaked real identifiers because `SagaAuthor` was fed the snippet-bearing reduced tagging-view. Every consequential decision below is RESOLVED on documented merits (this is autonomous — decide and proceed). Cited paths are to files read during analysis. **The load-bearing line: the Saga is now authored over the SAME name-free data the public bundle already ships — it can leak NOTHING the bundle doesn't already safely expose. Interpret/beats (5.7) + the committed fixture bundle are UNCHANGED.**

### §0 — Current state + the WHY (verified at HEAD `cdf5081`)

- **The leak (the WHY for AC1/AC2/AC3):** the `--cli`/`--real` bake does `saga = await author.authorSaga(taggingView)` (`scripts/build-bundle.ts:219`) where `taggingView = buildTaggingView(scrubResult.scrubbedEvents)` (`build-bundle.ts:186`). `SagaAuthor.authorSaga` `JSON.stringify`'s its input into the user message (`saga-author.ts:105`), so the prompt is the FULL serialized tagging-view. The tagging-view's per-event `snippet` (`src/bundle/tagging-view.ts:95-133`) is path-free + secret-scrubbed but NOT symbol/name-free: the Bash branch DELIBERATELY keeps the command intact (`tagging-view.ts:104-109` — "the command IS the beat signal", F1 of Story 5.6), so `snippet` carries `pnpm drizzle-kit …`, `psql … auth_tokens`, etc.; the `tool_result` head carries assistant verdicts (component/table names); the assistant-text head carries narrative naming the product. The Opus prose absorbed `Vibranto`, `auth_tokens`, `SaveProfileSheet`, `migration 0045`, `PUBLIC_BASE_URL`, `FK-23503`, Drizzle/Hono/zod, etc. (operator memory, this session). This crosses the Story 5.5 HARD LINE: the PUBLIC bundle (now incl. its Layer-2 Saga) must NEVER ship a real file/symbol/path/name.
- **What ships CLEAN already (the safe surface to author over):** the public `ReplayBundle` ships `projectedEvents` (`src/bundle/project-events.ts` — the Story 5.5 PAYLOAD-FREE, NAME-FREE, PATH-FREE projection: `{orderKey, eventId, eventType, toolName, outcome, role}`; `classifyRole` reads the path ONLY to derive a coarse token then DISCARDS it) and `annotations` (the frozen `BeatAnnotation[]` — `{eventRef, beatType, confidence, interpreterVersion, sourceHash, groundingPointer}`; the only strings are opaque ids + enums + hashes). The Story 5.7 bake proved BOTH are name-free. PLUS `teaching.json` (the shaman/dispel/summon plain-dev one-liners — safe authored narrative material). These three are the brief's ONLY inputs.
- **What must NOT move (verified):** interpret stays CHUNKED over the tagging-view (`build-bundle.ts:213-217`, Story 5.7 — UNTOUCHED); `annotationHash = sha256(normalizedEvents + interpreterVersion + promptVersion)` over the FULL scrubbed events (`freeze.ts` `freezeAnnotations`, called from `assemble-bundle.ts`) — `assembleBundle` is called with `scrubResult` UNCHANGED, so `annotationHash`/freeze are over the full events regardless of the Saga input. The committed `public/bundles/story-10-1.json` (`annotationHash` `c10c15aa…`, 14 projectedEvents, 2 annotations) — byte-identical after this story (the mocked path uses `PLACEHOLDER_SAGA`, never `SagaAuthor`).

### §1 — Why a PURE `buildSagaBrief` over the PUBLIC surface (the architecture)

The fix is structural, not a filter: rather than try to scrub names OUT of the tagging-view's snippets (an endless cat-and-mouse — names hide in commands, verdicts, prose), author the Saga over a surface that has NO names BY CONSTRUCTION. `projectEvents` (Story 5.5) already collapses every event to name-free `{eventType, toolName, role, outcome}` (it structurally drops payload/path/name — `classifyRole` discards the path; `tagging-view.ts` adds the leaky `snippet`, which is exactly what the projection omits). The frozen `annotations` carry only enums + opaque ids + hashes. `teaching.json` is authored safe narrative. So `buildSagaBrief({projectedEvents, annotations, teaching})` is name-free BY CONSTRUCTION — the leak is impossible, not merely filtered. **Decision:** the brief is a plain `interface` (NOT Zod) — it is a TRANSIENT in-memory bake-prompt input (never serialized to a shippable artifact, never read from an untrusted source — the `TaggingViewEvent`/`TeachingOp` precedent). It is PURE/deterministic (R2 posture, even though `src/scribe/` is not lint-bound to purity) so the gate proof (call-twice `toEqual`, no mutation) is clean. It lives in `src/scribe/` (the Layer-2/Told home — it is a Saga-authoring concern, the sibling of `saga-author.ts`), is SDK-free + phaser-free, and is browser-UNREACHABLE (only `scripts/*` + its own test import it), so `grep -ril anthropic dist/` stays empty.

### §2 — Brief field selection: minimal, name-free, arc-sufficient (the content decision)

The Saga needs the ARC, not the bytes (the Story 5.6 §8 principle, now applied to the name-free surface). **Decision (events):** keep `{eventType, toolName, role, outcome}` per projected event — DROP `orderKey`/`eventId` (opaque ids the Saga grounds NOTHING on; carrying them buys nothing and keeps the brief minimal). **Decision (beats):** keep `{beatType, confidence}` in order — DROP `eventRef`/`groundingPointer`/`sourceHash`/`interpreterVersion` (the Saga has no use for grounding ids or content addresses; the beat TYPE + order + confidence are the narrative spine — the shaman/dispel/summon arc). **Decision (teaching):** pass the validated `TeachingTable` verbatim (three safe one-liners — the shaman/dispel/summon concepts the Saga can lean on for register). These selections make the brief STRICTLY smaller than the already-public surface, so AC3's subset proof ("the brief is a derivative of what the bundle already ships publicly") holds by construction: every brief field is a field-restriction of `ProjectedEvent`/`BeatAnnotation`/`TeachingTable`, none traceable to `payload`/`snippet`/`path`.

### §3 — The OPTIONAL `arc` summary (decide on merits)

The createBrief offers "(optionally a name-free `battleTimeline` arc summary)". **Decision:** DEFAULT to OMITTING `arc` — the projected events (in order, with role/outcome) + the beats (the shaman/dispel/summon arc, in order) + the teaching concepts already give the Saga the full closing arc (the kingdom-spanning struggle → the binding-spell fix → the victory). A `battleTimeline` arc summary would add only name-free numeric aggregates (beat counts, a victory marker) that the beats already imply. IF the dev finds the Saga shape materially benefits from an explicit arc, include a MINIMAL `SagaBriefArc { beatCount: number }` derived ONLY from name-free aggregates (NEVER `sourceEventIds`/paths/names) — but the simpler events+beats+teaching brief is the preferred default. The `battleTimeline` builder arg stays OPTIONAL either way so the type supports a future arc without a signature change. Document whichever is chosen. (RECOMMENDATION for the dev: omit `arc`; revisit only if the operator's re-bake reads thin.)

### §4 — The `SagaAuthor` input type (minimal, intent-revealing)

`authorSaga` only `JSON.stringify`'s its input + makes no truth claim (`saga-author.ts:94-121`), so the input is OPAQUE to the author — the type just documents the real shape. At HEAD it is `SagaInput = readonly NormalizedEvent[] | readonly TaggingViewEvent[]` (the Story 5.6 widening for the tagging-view input). **Decision:** make `SagaBrief` the canonical input and DROP the `NormalizedEvent[] | TaggingViewEvent[]` union members — both production callers are rewired to pass a brief (Task 4), so the legacy shapes are dead, and removing the `TaggingViewEvent` import severs the leak-shaped type from the Saga path entirely (defense in depth — the leaky type can no longer even be passed). The existing `saga-author.test.ts`/`saga-author.unit.test.ts` are updated to pass a `buildSagaBrief(...)` (the AC4 task authorizes "updating `saga-author.test.ts` + the two callers"). Use `import type { SagaBrief } from './saga-brief'` (erased at runtime; `saga-brief.ts` is SDK-free anyway). If a remaining test genuinely needs a non-brief input, widen to `SagaBrief | …` rather than block — but prefer the clean single type.

### §5 — The anonymization clause (belt over the name-free input, AC2)

The brief is name-free by construction (the suspenders); the system-prompt clause is the BELT — it stops the model from FABRICATING a plausible real-looking identifier to fill an abstract gap (e.g. inventing a "users table" when it sees a `role: 'migration'` event). **Decision:** append to the existing `SYSTEM_PROMPT` (`saga-author.ts:65-72`) an explicit instruction: forbid any real product/project/file/module/class/function/table/column/env-var/library name; instruct fantasy/role/concept language ONLY; state that the input is abstract role/concept data and the model must not fabricate concrete identifiers. Keep it inline (the established module-constant pattern, overridable via `systemPrompt?`). **Decision (version stamp):** bump `DEFAULT_PROMPT_VERSION` `'saga-tolkien-v1' → 'saga-tolkien-v2'` so the operator's re-baked Saga records the anonymization-hardened prompt in its provenance metadata (`saga-author.ts:52`, `promptVersion`). This is metadata only and does NOT touch the committed bundle (the mocked path's `promptVersion` is `FIXTURE_PROMPT_VERSION = 'fixture-v1'`, set in `build-bundle.ts:79`/`233`, independent of `SagaAuthor`'s default). Update the single unit asserting the default (`saga-author.unit.test.ts:42`). The fail-loud empty-Saga guard (`saga-author.ts:114-119`) is PRESERVED verbatim (AC2).

### §6 — Why interpret/beats stay UNCHANGED (the divergence, AC5)

The interpret prompt and the Saga input now DIVERGE — and that is correct. The INTERPRETER needs the per-event snippet (the tagging-view) to recognize a beat (a Bash command head IS the Shaman/Dispel signal — Story 5.6 F1); it is a transient bake input that is NEVER shipped/hashed/logged-by-default, and its output (the annotations) is already name-free. The SAGA produces SHIPPED PROSE, so it must author over only the name-free public surface. **Decision:** leave the interpret path EXACTLY as Story 5.7 left it — `interpretChunked({ promptView: taggingView, groundingEvents: scrubResult.scrubbedEvents, … })` (`build-bundle.ts:213-217`) is UNTOUCHED; `tagging-view.ts` STAYS (it is still the interpret prompt input, just no longer the Saga input). The ONLY changes are the Saga input (the brief) + the system-prompt clause. `annotationHash`/freeze (`assemble-bundle.ts`/`freeze.ts`) are UNCHANGED — `assembleBundle` is still called with `scrubResult`. This keeps Story 5.7's full-session bake enablement intact while closing the Saga leak.

### §7 — Why this is safe + surgical (the inert-at-replay proof)

This story touches the Saga AUTHORING INPUT + SYSTEM PROMPT only. At replay nothing changes: the browser loads the same payload-free `projectedEvents` bundle, folds the same baked timeline, resolves the same abstracted grounding, reads the same (placeholder, until the operator re-bakes) `saga` field. The brief + clause live entirely in the offline `scripts/` + `src/scribe/` path. interpret/beats/`annotationHash`/freeze are UNCHANGED (Story 5.7 intact). The committed fixture bundle is byte-identical (the mocked path uses `PLACEHOLDER_SAGA`, never `SagaAuthor`/`buildSagaBrief`). BOTH golden snapshots are byte-stable (no Layer-0 code added). R1/R4/R5 hold (`saga-brief.ts` is SDK-free, phaser-free, `src/scribe/`-resident, importing only `schema/` types + `portal/teaching-config` value + optionally `bundle/project-events` — the `scribe → bundle`/`scribe → portal` edges are eslint-allowed, the `saga-author.ts:2 → bundle` precedent). The `eslint.config.ts` is UNTOUCHED.

### Project Structure Notes

- **New files:** `src/scribe/saga-brief.ts` (the PURE name-free brief builder) + `src/scribe/saga-brief.test.ts` (AC1/AC3 — shape, determinism, no-mutation, the NAME-FREE serialized-brief proof, the SUBSET proof, SDK/phaser-free source-grep).
- **Modified files:** `src/scribe/saga-author.ts` (the anonymization clause appended to `SYSTEM_PROMPT`; `SagaInput` → `SagaBrief`; `DEFAULT_PROMPT_VERSION` → `'saga-tolkien-v2'`; doc comments); `src/scribe/saga-author.test.ts` (rebuild the input as a `buildSagaBrief(...)`; add the anonymization-clause + name-free-input cases); `src/scribe/saga-author.unit.test.ts` (update the default-promptVersion assertion to `'saga-tolkien-v2'`); `scripts/build-bundle.ts` (the `--cli`/`--real` Saga input → `buildSagaBrief({projectedEvents: projectEvents(scrubbedEvents), annotations, teaching: TEACHING})`; header doc); `scripts/scribe-saga.ts` (the standalone baker → `buildSagaBrief({projectedEvents: projectEvents(events), annotations: [], teaching: TEACHING})`; §3 decision); `_bmad-output/implementation-artifacts/sprint-status.yaml` (`5-8` backlog → ready-for-dev; `last_updated`).
- **Untouched (load-bearing, verify byte-stable):** `src/interpret/chunked-interpret.ts`, `src/interpret/claude-interpreter.ts`, `src/interpret/freeze.ts`, `src/interpret/fixture-interpreter.ts` (Story 5.7/5.6/3.2 — AC5); `src/bundle/tagging-view.ts` (STAYS the interpret prompt input — Story 5.6); `src/bundle/project-events.ts` (REUSED AS-IS — Story 5.5); `src/bundle/assemble-bundle.ts` (called with `scrubResult` unchanged); `src/schema/replay-bundle.ts` (shipped bundle shape — AC5); `src/config/teaching.json` + `src/portal/teaching-config.ts` (CONSUMED AS-IS); `eslint.config.ts`; all Layer-0 modules + both golden snapshots; `public/bundles/story-10-1.json` (byte-identical).
- **Conventions (project):** kebab-case files; `SagaBrief`/`SagaBriefEvent`/`SagaBriefBeat` PascalCase types; reuse the Story 5.5 `Outcome`/`AbstractedRole` + `BeatType` literal unions (one source of truth); co-located `*.test.ts`; plain `interface` (NOT Zod) for the transient in-memory brief (the `TaggingViewEvent`/`TeachingOp` precedent); PURE builder (R2 posture); prefer explicit field-restriction over carrying ids the Saga grounds nothing on.

### References

- [Source: `_bmad-output/planning-artifacts/architecture.md`#LLM Integration L181-190] — interpreter = claude-sonnet-4-6 via structured/tool output; Saga = claude-opus-4-8; offline/build-time only (R4).
- [Source: `_bmad-output/planning-artifacts/architecture.md`#R1-R5 L224-241; #Anti-Patterns L294-299] — provenance/purity/anti-corruption/LLM-isolation/RenderPort rules; the Layer-2 (Told) discipline.
- [Source: `_bmad-output/planning-artifacts/epics.md`#Story-4.2 L448 "Pre-generated closing Saga"; #Story-5.2 L519-533] — the offline `SagaAuthor` + the bundle-orchestration bake seam this story rewires.
- [Source: `src/scribe/saga-author.ts` L65-121] — the `SYSTEM_PROMPT` (the anonymization-clause target, L65-72), `authorSaga` (the input `JSON.stringify`, L105; the fail-loud guard, L114-119), `SagaInput` (the type to narrow, L10), `DEFAULT_PROMPT_VERSION` (L52).
- [Source: `src/bundle/project-events.ts` L24-57] — `projectEvents` (the Story 5.5 PAYLOAD-FREE, NAME-FREE projection the brief reuses) + `resolveTargetPath` (path read then DISCARDED).
- [Source: `src/bundle/tagging-view.ts` L95-133] — the snippet-bearing tagging-view (the LEAK SOURCE; the Bash branch keeps the command intact, L104-109) — REMOVED from the Saga path, STAYS the interpret prompt input.
- [Source: `src/config/teaching.json`; `src/portal/teaching-config.ts` L1-55] — the shaman/dispel/summon teaching one-liners + the validated `TEACHING`/`TeachingTable` the brief passes through.
- [Source: `src/schema/replay-bundle.ts` L29-90] — the public `ProjectedEvent` (`Outcome`/`AbstractedRole` unions) + the shipped bundle shape (untouched).
- [Source: `src/schema/beat-annotation.ts` L1-29] — the `BeatAnnotation` (`beatType`/`confidence` the brief keeps; `eventRef`/`groundingPointer`/`sourceHash` it drops) + `BeatType`.
- [Source: `scripts/build-bundle.ts` L175-234] — the `--cli`/`--real` Saga wiring (`author.authorSaga(taggingView)`, L219) + the mocked `else` branch (`PLACEHOLDER_SAGA`, L227); `interpretChunked` (L213-217, UNTOUCHED); `FIXTURE_PROMPT_VERSION` (L79/233).
- [Source: `scripts/scribe-saga.ts` L100-115] — the standalone Saga baker feeding full payload-bearing events (rewire target).
- [Source: `eslint.config.ts` L94-110] — R4/R5 zones (anthropic re-allowed in `scribe/`; no scribe→bundle/portal restriction — the `saga-author.ts:2 → bundle` precedent).
- [Source: `src/scribe/r1-discipline.test.ts`] — the SDK-free + phaser-free source-grep for browser-reachable scribe READERS (`saga-brief.ts` is NOT added — it is a bake-input builder like `saga-author.ts`; its SDK/phaser-freedom is asserted in `saga-brief.test.ts`).
- [Source: `_bmad-output/implementation-artifacts/5-5-payload-free-bundle.md`] — the payload-free projection + the operator HARD LINE this story extends to Layer 2.
- [Source: `_bmad-output/implementation-artifacts/5-6-reduced-tagging-view-multistream.md`; `5-7-chunked-interpret.md`] — the tagging-view + chunked-interpret bake-input machinery (UNTOUCHED; the Saga is removed from the tagging-view path).

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (BMAD dev-story autonomous run, red-green over the prior ATDD red tests).

### Debug Log References

- RED confirmed: `pnpm vitest run src/scribe/saga-brief.test.ts src/scribe/saga-author.name-safe.test.ts` → both FAIL on `Cannot find module './saga-brief'` (module resolution, the red half).
- Baseline: full suite 107 files / 1123 tests GREEN at HEAD (the 2 red files failing) — matches the story baseline.
- One typecheck miss caught + fixed mid-run: `src/schema/replay-bundle.ts` exports `AbstractedRoleSchema` but NOT a named `AbstractedRole` type (and `Outcome` lives there, `AbstractedRole` in `bundle/classify-role.ts`). Resolved by deriving both member types from `ProjectedEvent['role']`/`['outcome']` (indexed access) inside `saga-brief.ts` — single source of truth, zero drift, no second import path.
- GREEN: the 4 saga test files (saga-brief / saga-author.name-safe / saga-author / saga-author.unit) = 43 tests pass.
- Bundle byte-identity: `pnpm bundle:story-10-1` → sha256 `566fbaaf…` (unchanged), annotationHash `c10c15aa…` (unchanged), `git diff` empty, Saga = PLACEHOLDER. Two runs byte-identical (determinism).

### Completion Notes List

Implemented the name-safe Saga (build-surfaced privacy fix from the Story 5.7 real bake). The Saga is now authored over the SAME name-free data the public bundle already ships, so Layer-2/Told can leak nothing the bundle does not already safely expose.

- **Task 1 (AC1/AC3):** NEW `src/scribe/saga-brief.ts` — PURE, deterministic, SDK-free + phaser-free `buildSagaBrief({projectedEvents, annotations, teaching, battleTimeline?})` returning a plain-`interface` `SagaBrief` `{events:[{eventType,toolName,role,outcome}], beats:[{beatType,confidence}], teaching}`. Drops orderKey/eventId (events) + eventRef/groundingPointer/sourceHash/interpreterVersion (beats) per §2; passes the validated `TeachingTable` through verbatim. **DECISION (§3): `arc` OMITTED** — events (in order) + beats (the shaman/dispel/summon arc) + teaching already carry the full closing arc; the `battleTimeline` arg stays OPTIONAL so a future arc needs no signature change (currently read-but-never-carried, so no name can leak even if added).
- **Task 2 (AC2):** Appended the anonymization clause to `SagaAuthor.SYSTEM_PROMPT` (forbids emitting/inventing any real product/project/file/module/class/function/table/column/env-var/library name; fantasy/role/concept language ONLY; do not fabricate a real name to fill an abstract gap). Tolkien register + Forgemaiden cry + fail-loud empty-Saga guard PRESERVED verbatim. **DECISION (§5): bumped `DEFAULT_PROMPT_VERSION` `saga-tolkien-v1`→`saga-tolkien-v2`** (the system prompt materially changed; the re-bake's provenance must reflect the hardened prompt). Metadata only — committed bundle unaffected (mocked path stamps `FIXTURE_PROMPT_VERSION='fixture-v1'`).
- **Task 3 (AC4):** **DECISION (§4): narrowed `authorSaga`'s input from `SagaInput = NormalizedEvent[]|TaggingViewEvent[]` to `SagaBrief`** (the canonical input; both production callers now pass a brief), dropping the leak-shaped `TaggingViewEvent` import from the Saga path entirely (defense in depth). Updated `saga-author.test.ts` (input rebuilt as `buildSagaBrief(...)` over the fixture's public surface via `projectEvents` + `fixtureAnnotations`; the "event window" test now asserts the BRIEF shape, no eventId/snippet; ADDED an AC2 anonymization-clause case + an AC1/AC4 name-free-prompt case) and `saga-author.unit.test.ts` (EMPTY_BRIEF input; default promptVersion → `saga-tolkien-v2`; the user-content assertion now matches the serialized brief object).
- **Task 4 (AC1/AC4/AC5):** Rewired both callers. `scripts/build-bundle.ts` `--cli`/`--real`: Saga input → `buildSagaBrief({ projectedEvents: projectEvents(scrubResult.scrubbedEvents), annotations, teaching: TEACHING, battleTimeline })`. The interpret path (`interpretChunked` over the tagging-view, grounding over full scrubbed events) + `assembleBundle(scrubResult,…)` are UNCHANGED — the tagging-view STAYS the interpret prompt, removed ONLY from the Saga path (the two diverge). The mocked `else` branch (`PLACEHOLDER_SAGA`) is untouched. `scripts/scribe-saga.ts`: `--cli`/`--real` → `buildSagaBrief({ projectedEvents: projectEvents(events), annotations: [], teaching: TEACHING })`. **DECISION (§3): beats-less brief, NO new scrub step** — `projectEvents` is name-free BY CONSTRUCTION (drops payload/path/name) so the standalone baker needs no scrub at the Saga boundary; build-bundle.ts is the canonical bake WITH real beats. Default no-flag PLACEHOLDER path untouched.
- **Task 5 (gates):** `pnpm typecheck` clean (strict); `pnpm lint` clean (R1/R4/R5 hold, `eslint.config.ts` UNTOUCHED); `pnpm test` GREEN — 109 files / 1146 tests (was 107/1123: +2 ATDD files, +23 tests); `pnpm build` OK; `grep -ril anthropic dist/` = NOTHING (R4); committed `public/bundles/story-10-1.json` BYTE-IDENTICAL to HEAD (`git diff` empty); two bundle runs byte-identical; both golden snapshots byte-stable. AC5: `git diff HEAD` shows interpret/freeze/fixture/assemble/tagging-view/replay-bundle UNTOUCHED. Did NOT `git commit` (operator commits). The real Saga RE-BAKE (billed) is the deferred operator step.

### File List

- `src/scribe/saga-brief.ts` (NEW — the PURE name-free Saga brief builder + `SagaBrief`/`SagaBriefEvent`/`SagaBriefBeat` types)
- `src/scribe/saga-brief.test.ts` (NEW — ATDD red test, now GREEN; pre-existing in tree)
- `src/scribe/saga-author.name-safe.test.ts` (NEW — ATDD red test, now GREEN; pre-existing in tree)
- `src/scribe/saga-author.ts` (MODIFIED — anonymization clause in SYSTEM_PROMPT; `SagaInput`→`SagaBrief`; `DEFAULT_PROMPT_VERSION`→`saga-tolkien-v2`; doc comments)
- `src/scribe/saga-author.test.ts` (MODIFIED — input rebuilt as `buildSagaBrief(...)`; brief-shape + anonymization-clause + name-free-prompt cases)
- `src/scribe/saga-author.unit.test.ts` (MODIFIED — EMPTY_BRIEF input; default promptVersion → `saga-tolkien-v2`)
- `scripts/build-bundle.ts` (MODIFIED — `--cli`/`--real` Saga input → name-free brief; static `projectEvents`/`buildSagaBrief`/`TEACHING` imports; header doc)
- `scripts/scribe-saga.ts` (MODIFIED — standalone baker → name-free beats-less brief; static imports; header doc)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (MODIFIED — `5-8` ready-for-dev → in-progress → review; `last_updated`)
- `_bmad-output/implementation-artifacts/5-8-name-safe-saga.md` (MODIFIED — task checkboxes, Dev Agent Record, File List, Status → review)

## Senior Developer Review (AI)

**Reviewer:** Andres Felipe Grisales (AI-assisted triage)
**Date:** 2026-06-15
**Outcome:** Approve with one privacy follow-up. The structural name-free pivot is sound and well-built; the residual is the `toolName` channel for MCP/custom tools, which is NOT name-free by construction (only secrets/paths/emails are scrubbed). The AC2 anonymization clause is a soft mitigation, not a hard guarantee.

### Verdict

The Saga is now authored over the payload-free public-surface brief (`buildSagaBrief` over `projectedEvents` + frozen beats + teaching), severing the snippet-bearing tagging-view from the Saga path. AC1–AC5 hold as implemented. typecheck clean, saga suite GREEN (80/80), committed bundle byte-identical (not in `git diff HEAD`), interpret/beats/assemble/tagging-view untouched. The one real gap: the "name-free BY CONSTRUCTION" claim is overstated for sessions that use MCP/custom tools, because `toolName` (e.g. `mcp__clickup__create_task`) is carried verbatim and no scrub pattern matches it.

### Findings

- **[F1] med — `toolName` is NOT name-free by construction for MCP/custom tools (`fix`/consider).** Location: `src/scribe/saga-brief.ts:77`, `src/bundle/project-events.ts:53`, also reachable via `scripts/scribe-saga.ts:63-65` and `scripts/build-bundle.ts:233-238`. Layers: EDGE_BOUNDARY_HUNTER (x2, merged). VERIFIED: `src/ingest/allowlist.ts:22` keeps ALL tool_use names (`keepAllToolUse:true`); `src/ingest/normalize.ts:95` sets `toolName = item.name`; `projectEvents` carries it verbatim; `src/config/scrub-patterns.json` only matches secrets/tokens/credentials/emails/home-paths/2 DB roles — none match `mcp__<product>__<action>`. So an MCP tool name (carrying a product/integration identifier) survives scrub and reaches the serialized brief → the Saga prompt, on BOTH the build-bundle (scrub-first) and scribe-saga (un-scrubbed) paths. Built-in fixtures keep tests green and the committed bundle byte-identical, so this is latent until a real MCP session is baked. AC2's clause tells the model not to emit real product names, but that is a behavioral belt, not the structural guarantee the story repeatedly asserts. Recommendation: consider (lean fix). Suggested fix: in `buildSagaBrief`, map non-built-in / `mcp__*` toolNames to a neutral token (e.g. `'tool'`) before carrying, and add a fixture with an `mcp__*` toolName asserting it does not reach the serialized brief.

- **[F2] low — `battleTimeline` arg accepted but never destructured/used (`likely-refute`).** Location: `src/scribe/saga-brief.ts:70,72`; pass site `scripts/build-bundle.ts:237`. Layers: BLIND_HUNTER, ACCEPTANCE_AUDITOR. VERIFIED intentional per Dev Notes §3 (forward-compat seam) and SAFE for name-safety — `battleTimeline` carries `sourceEventIds` but the builder reads nothing from it, so no id can leak. Not a defect; the omission is itself the safety property. Recommendation: likely-refute (no change).

- **[F3] low — `scribe-saga.ts` `DEFAULT_PROMPT_VERSION` still `saga-tolkien-v1` (`consider`).** Location: `scripts/scribe-saga.ts:37,103`. Layer: BLIND_HUNTER. VERIFIED: only the no-flag PLACEHOLDER path stamps this (line 103); the `--cli`/`--real` paths correctly use `author.promptVersion` (= v2). On the placeholder path no anonymization-clause prompt actually runs, so the stamp is cosmetic — but it is inconsistent with `SagaAuthor`'s bumped default. Recommendation: consider (trivial one-line bump to `saga-tolkien-v2`, or a one-line comment documenting the intentional decoupling).

- **[F4] low — `teaching` passed through by reference, not cloned (`likely-refute`).** Location: `src/scribe/saga-brief.ts:88`. Layer: BLIND_HUNTER. VERIFIED: the builder does not mutate; the no-mutation test (`saga-brief.test.ts:162-172`) passes; the brief is transient and only `JSON.stringify`'d. The shared alias is harmless in the actual data flow. Recommendation: likely-refute (no change).

- **[F5] low — name-free path-marker test bans `'/'` while teaching one-liners flow through verbatim (`consider`).** Location: `src/scribe/saga-brief.test.ts:47,184-186` vs `202-213`. Layer: EDGE_BOUNDARY_HUNTER. VERIFIED: today's three teaching strings contain none of the banned markers so both assertions pass, but `teaching.json` is operator-editable config; a future one-liner containing `'/'` (e.g. a date or "and/or") would fail the leak proof on SAFE authored prose. Latent test-fragility, not a current defect. Recommendation: consider (scope the path-marker assertions to the events/beats portion, or exclude the known teaching strings from the substring check).

- **[F6] low — `scribe-saga.ts` standalone brief is beats-less and built over an un-scrubbed projection (`likely-refute`).** Location: `scripts/scribe-saga.ts:63`. Layer: ACCEPTANCE_AUDITOR. VERIFIED: name-safe via `projectEvents` (structurally drops payload/path/name) and explicitly documented as the §3 PREFERRED minimal choice, with build-bundle.ts as the canonical bake-with-beats. The only residual on this path is the same `toolName` channel already captured by F1. Recommendation: likely-refute (the scope decision is documented; toolName handled under F1).

### AC Summary

- **AC1 (name-free PURE brief):** PASS — `saga-brief.ts:66-90` keeps only the field-restricted public surface; pure/deterministic/no-mutation; load-bearing serialized-leak proof over fixtures with planted identifiers in dropped fields. Caveat: F1 (`toolName` for MCP tools is the one non-name-free channel).
- **AC2 (anonymization clause + fail-loud guard):** PASS — clause added at `saga-author.ts`, version bumped v1→v2, empty-Saga guard preserved, unit test asserts v2.
- **AC3 (brief ⊆ public surface):** PASS — every brief field is a field-restriction of ProjectedEvent/BeatAnnotation/TeachingTable; leak-prone beat fields dropped. Same F1 caveat (toolName is a public-surface field that is itself imperfectly name-free for MCP tools).
- **AC4 (committed bundle byte-identical):** PASS — `public/bundles/*` not in `git diff HEAD`; mocked path uses PLACEHOLDER_SAGA and builds no brief; typecheck clean.
- **AC5 (5.7 interpret/beats unchanged):** PASS — `git diff HEAD --name-only` shows interpret/freeze/fixture/assemble/tagging-view/replay-bundle untouched.

### Review Action Items — Resolution (fix round 1, 2026-06-15)

- **[F1] RESOLVED** — `toolName` is now name-free by construction (non-built-in / `mcp__*` names neutralized to `'tool'` in `buildSagaBrief`; fixture test added). The AC1/AC3 `toolName` caveats above are closed.
- **[F3] RESOLVED** — `scribe-saga.ts` `DEFAULT_PROMPT_VERSION` bumped to `saga-tolkien-v2` (in lockstep with `SagaAuthor`).
- **[F5] RESOLVED** — path-marker leak proof scoped to the events/beats portion (teaching one-liners excluded), so editing `teaching.json` cannot break the proof.
- **[F2] REFUTED** — `battleTimeline` arg is an intentional, SAFE forward-compat seam (read by nothing; `sourceEventIds` cannot leak). No change.
- **[F4] REFUTED** — `teaching` pass-by-reference is harmless (no mutation, transient, only `JSON.stringify`'d). No change.
- **[F6] REFUTED** — beats-less / un-scrubbed standalone brief is name-safe via `projectEvents` (documented §3); the only residual was the `toolName` channel, closed under F1. No change.

### Change Log

- 2026-06-15 — fix round 1 (Senior Developer Review (AI) resolution). Closed F1 (the `toolName` MCP/custom leak channel) by neutralizing non-built-in tool names to `'tool'` in `buildSagaBrief`; bumped `scribe-saga.ts` `DEFAULT_PROMPT_VERSION` to v2 (F3); scoped the name-free path-marker test to the events/beats portion (F5); refuted F2/F4/F6 with reasoning. Gates GREEN; committed bundle byte-identical; AC5 files untouched. Status → done.

### Completion Note (fix round 1)

All review findings addressed. The med-severity F1 — the one channel that was NOT name-free by construction (`toolName` carries `mcp__<product>__<action>` / custom names verbatim, unmatched by any scrub pattern) — is now closed structurally: `buildSagaBrief` collapses any non-built-in tool name to the neutral `'tool'` token, so the "name-free BY CONSTRUCTION" claim now holds for `toolName` too (not just a behavioral belt). F3/F5 are small consistency/test-robustness fixes. F2/F4/F6 are confirmed false positives (refuted). Gates: `pnpm typecheck` clean, `pnpm lint` clean (R1/R4/R5 intact, `eslint.config.ts` untouched), `pnpm test` 1147 passing (+1 F1 test), `pnpm build` OK with `dist/` anthropic-free (R4), committed `public/bundles/story-10-1.json` byte-identical (`annotationHash c10c15aa…`). Did NOT `git commit` (operator commits between stories).
